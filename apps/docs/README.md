# Arqel — Documentation site

VitePress 1.x site servido em `arqel.dev`.

```bash
pnpm install
pnpm dev       # http://localhost:5173
pnpm build     # estático em .vitepress/dist
pnpm preview
```

Veja `SKILL.md` para o contexto completo (estrutura, status DOCS-001..008, conventions).
