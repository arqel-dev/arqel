# Changelog

Todas as alterações notáveis a este projeto são documentadas neste arquivo.

O formato é baseado em [Keep a Changelog](https://keepachangelog.com/pt-BR/1.1.0/),
e este projeto adere ao [Semantic Versioning](https://semver.org/lang/pt-BR/).

## [Unreleased]

_Placeholder para próximo ciclo (pós v0.8.0-rc.1). Adicionar entradas conforme novos batches forem mesclados._

---

## [0.8.0-rc.1 — pendente de tag]

> Conteúdo abaixo será movido para a seção `## [0.8.0-rc.1] - YYYY-MM-DD` no momento da tag, conforme `docs/release-checklist-v0.8.md`.

### Added

- **Batch paralelo #34** (3 sub-agentes em worktrees, **3/3 entregues auto-completos**, merged 2026-04-30) — **Fase 3 progress slice 7**: AI-009 PHP slice (`packages/ai/src/Fields/AiSelectField.php` final extends `Arqel\Fields\Field` — setters fluent `options(array<string,string>)/classifyFromFields/prompt(string|Closure)/provider/aiOptions/fallbackOption(?string)`; `classify(formData)` constrói prompt completo "Available categories (key: label):...\n\nReply with ONLY the category key, nothing else", chama `app(AiManager::class)->complete()`, normaliza output via `trim() + lowercase + strip aspas/pontuação`, valida contra `$this->options` retornando key válida ou `fallbackOption` (default null) — **graceful fallback** quando AI retorna chave inválida; `getTypeSpecificProps()` expõe `options/classifyFromFields/provider/fallbackOption/hasContextFields` mas **NUNCA** o prompt template; `Http/Controllers/AiClassifyController` POST `/admin/{resource}/fields/{field}/classify` retorna `{key, label}`, 404 sem registry, 422 wrong field type; **11 Pest tests** novos (8 unit + 3 feature) — total `arqel/ai` 84 testes; nova fixture `ConfigurableFakeProvider` com `public string $textToReturn` para isolated testing). AI-008 React slice (**`packages-js/ai/src/AiTranslateInput.tsx`** componente functional — tablist `<button role="tab">` por idioma com `data-missing` + dot visual nas tabs com translation vazia/null; painel ativo com textarea controlado editando `translations[activeLang]`; botão "Translate from {default}" só renderizado em non-default tabs; botão "Translate all missing" no toolbar com `disabled` quando `missingCount === 0`; loading per-language via `Set<string>`; banner `role="alert"` em falha; suporta controlled (`onChange`) E uncontrolled fallback; SSR-safe; click handler POST com `{sourceLanguage, targetLanguages: [...], sourceText}` body, mergeia response em state; csrfToken em header `X-CSRF-TOKEN`; `register.tsx` adapter cast do onChange (registry tipa `(value: string) => void` mas este field emite `Record<string, string>`); **11 vitest tests** com `vi.stubGlobal('fetch')` cobrindo tabs/active switch/textarea edit/conditional translate button/fetch body/merge response/error banner/translate-all only-missing/visual indicators/csrfToken/translateUrl override; total `@arqel/ai` 21 testes; biome 1 fix a11y `role="img"` no dot span). WF-008 (`packages/workflow/src/Filters/StateFilter.php` final readonly — `__construct(field, modelClass)` valida via `class_uses_recursive` que model usa trait `HasWorkflow` (lança `InvalidArgumentException` defensive); `make()` factory; `toArray()` shape `{field, type:'state', label:'State', options}`; `optionsArray()` keyed por state class/token retornando `{value, label, color, icon}` lendo metadata via `WorkflowDefinition::getStateMetadata()`; `apply(Builder, $value)` — string→`where`, array→`whereIn`, null/empty/`[]`→no-op; `Filters/StateFilterFactory::forResource($modelClass, ?$field)` resolve field automaticamente via `arqelWorkflow()->getField()` quando omitido; **decisão arquitetural** — `composer.json` permanece SEM `arqel/table` em require, `StateFilter` expõe shape genérico que user-land pluga em `Table::filters([...])` preservando design standalone do workflow pkg; **12 Pest tests** novos cobrindo factory/shape/options/apply variants/throws defensives (sem trait, classe inexistente, field vazio)/factory com/sem override; total workflow 58 testes; PHPStan max clean (2 calls dinâmicas a `arqelWorkflow()` em base Model usaram `@phpstan-ignore method.notFound` cobertas pela validação de trait no construtor — exceção justificada porque o trait é duck-typed e PHPStan não reconhece via `class_uses_recursive`); SKILL.md secção "Entregue (WF-008)" PT-BR com exemplo user-land plug-in). **Total cumulative Fase 3:** 84 ai + 35 realtime + 58 workflow PHP + 17 workflow JS + 19 realtime JS + 21 ai JS = ~234 testes diretos.
- **Batch paralelo #33** (3 sub-agentes em worktrees, **3/3 entregues auto-completos**, merged 2026-04-30) — **Fase 3 progress slice 6**: AI-008 PHP slice (`packages/ai/src/Fields/AiTranslateField.php` final extends `Arqel\Fields\Field` — setters fluent `languages(array)/defaultLanguage(string, lança InvalidArgumentException se não está em languages)/autoTranslate(bool)/provider/aiOptions`; `translate(sourceText, targetLanguage, ?sourceLanguage)` chama `app(AiManager::class)->complete()` com prompt "Translate the following text from {source} to {target}"; `translateAll(translations, sourceLanguage)` itera sobre `$this->languages` e preenche apenas keys ausentes/vazias, **skipa o sourceLanguage**; `getTypeSpecificProps()` expõe `languages/defaultLanguage/autoTranslate/provider` — NÃO inclui prompt template; `Http/Controllers/AiTranslateController` single-action POST `/admin/{resource}/fields/{field}/translate` aceita `{sourceLanguage, targetLanguages: string[], sourceText}`, resolve resource via FQCN-string defensive 404, 422 quando field não é AiTranslateField, `Gate::has('use-ai')` opt-in; route `arqel.ai.translate` em `packages/ai/routes/web.php`; **11 Pest tests** (8 unit + 3 feature); total `arqel/ai` 73 testes; React `AiTranslateInput.tsx` deferido). AI-007 React slice (**novo pacote `packages-js/ai/`** = `@arqel/ai@0.10.0-rc.1` — package.json com peers `react ^19.2.0` + `@inertiajs/react ^3.0.0`, sem deps novas; `src/AiTextInput.tsx` componente functional controlled-or-uncontrolled `<textarea>` + botão "Generate with AI" (label vem de `props.buttonLabel`, alterna para "Regenerate" após primeira geração bem-sucedida); inline SVG spinner + disabled state durante fetch; `role="alert"` error banner com HTTP status code; char counter `X / maxLength` quando maxLength presente; SSR-safe (não toca `window`/`document` no render path); click handler POST a `props.generateUrl ?? \`/admin/${resource}/fields/${field}/generate\`` com header `X-CSRF-TOKEN`, `credentials: 'same-origin'`, body `{ formData }`, parse `{ text }` do JSON response e chama `onChange(text)` ou setState interno fallback; `src/register.tsx` lazy adapter wrapping registry shape `{ field, value, onChange, resource, formData, csrfToken }` em props do componente; **10 vitest tests** com `vi.stubGlobal('fetch')` cobrindo initial value/onChange typing/default URL build/loading state/success injection/failure alert+status/char counter/Regenerate label flip/csrfToken propagation/generateUrl override; SKILL.md PT-BR canônico). WF-007 (`packages/workflow/database/migrations/2026_05_01_000000_create_arqel_state_transitions_table.php` schema com `morphs('model')`, `from_state` nullable, `to_state`, `transitioned_by_user_id` (indexed), `metadata` JSON nullable, `created_at` `useCurrent()`, sem `updated_at` — audit é **append-only**; `Models/StateTransition` final Eloquent com `$timestamps=false`, casts `metadata=>array, created_at=>datetime`, relationships `model(): MorphTo` + `user(): ?BelongsTo` defensive lendo `arqel-workflow.user_model`; `Listeners/PersistStateTransitionToHistory` final registado em `WorkflowServiceProvider::packageBooted()` via `Event::listen` SOMENTE quando `history.enabled=true`, skip silencioso + try/catch defensivo; `HasWorkflow::stateTransitions(): MorphMany` ordenado `created_at desc, id desc` — segundo critério desambigua transições no mesmo segundo em sqlite; `StateTransitionField::resolveHistory()` agora retorna entries reais (substituindo o TODO de WF-003) com query `model_type/model_id` filtro + limit configurável + try/catch para tabela ausente, cada row `{from, to, at (ISO 8601), by, metadata}`; config bloco `history{enabled, limit}`; **7 Pest tests** novos cobrindo persistência/JSON cast/relationship/disabled skip/order desc/resolveHistory non-empty/empty-no-record; total workflow 46 testes; PHPStan max clean, Pint clean). **Total cumulative Fase 3:** 73 ai + 35 realtime + 46 workflow PHP + 17 workflow JS + 19 realtime JS + 10 ai JS = ~200 testes diretos.
- **Batch paralelo #32** (3 sub-agentes em worktrees, **3/3 entregues** — A32 e B32 auto-completos, C32 recovery manual, merged 2026-04-30) — **Fase 3 progress slice 5**: AI-007 PHP slice (`packages/ai/src/Fields/AiTextField.php` final extends `Arqel\Fields\Types\TextField` — TextareaField era `final`, escolhido TextField como base; setters fluent `prompt(string|Closure)/provider/aiOptions/contextFields/maxLength/buttonLabel`; `resolvePrompt(array $formData)` público interpola `{fieldName}` placeholders ou executa Closure; `generate()` chama `app(AiManager::class)->complete()` e trunca em `maxLength-1` + `…` se exceder; `getTypeSpecificProps()` exclui o prompt template (segurança — pode conter business logic); `Http/Controllers/AiGenerateController` single-action com `Gate::has('use-ai')` opt-in (allow por default), retorna 200/404/422 conforme spec; route `POST /admin/{resource}/fields/{field}/generate` middleware `web,auth` named `arqel.ai.generate`; `AiServiceProvider::configurePackage()` ganhou `->hasRoute('web')`; **adicionado `arqel/fields: @dev`** ao composer + path-repo `../fields`; **11 Pest tests** novos (8 unit + 3 feature) — total `arqel/ai` 62 testes verdes; React `AiTextInput.tsx` explicitamente deferido para batch futuro). RT-010 (`packages-js/realtime/src/useConnectionStatus.ts` hook React subscreve `window.Echo.connector.pusher.connection` para eventos `connected/connecting/disconnected/failed/unavailable`, expõe `{status, lastConnectedAt, retryCount}` — SSR-safe via `typeof window`, polling defensivo 250ms x 5s para subscrição tardia quando Echo aparece após o mount, degrada para `'unavailable'` se nunca aparecer; `useFallbackPolling.ts` dispara `router.reload({ only })` no intervalo configurado quando `enabled === true` (default 30000ms) — estabiliza `only` array via key serializada para evitar re-execução por referência; `ConnectionStatusBanner.tsx` componente combina ambos os hooks: render `null` para `connected`/`unavailable`, banner `role="status" aria-live="polite"` com mensagens default por status, aceita `renderBanner?: (status, retryCount) => ReactNode` custom, `pollOnDisconnect` ativa fallback apenas durante `'disconnected'`; **15 vitest tests** novos (19 total) cobrindo SSR-safety/bind-unbind Pusher events/state transitions/retryCount/late-mount polling/cleanup/render conditional/seletivo polling; devDeps `@inertiajs/react`/`@testing-library/react`/`react-dom` adicionados; SKILL.md secção "Connection resilience (RT-010)" PT-BR). WF-006 (`packages/workflow/src/Authorization/TransitionAuthorizer.php` final readonly — autorização central com 3 camadas: (1) `authorizeFor(?Authenticatable, mixed): bool` na transition class (estático ou instância — preferido — exceções degradam para `false`, fail closed), (2) Gate `transition-{fromSlug}-to-{toSlug}` via `Gate::has + forUser->allows`, (3) **deny by default** com flag opt-out `arqel-workflow.authorization.deny_when_undefined => false` para preservar legado WF-003; helper público `slugifyState()` kebab-case do segmento final do FQCN sufixos `State` removidos; `StateTransitionField::resolveAvailableTransitions()` atualizado para delegar ao Authorizer em vez de chamar Gate diretamente; **breaking change** vs WF-003 documentado em SKILL — comportamento original retornava `true` quando ausência de Gate, agora deny-by-default; assinatura `authorize(string $transitionClass, ...)` aceita string genérica (não `class-string`) — método valida via `class_exists`, devolve `false` para inputs inválidos, evita callers terem que provar class-string; **10 Pest tests** novos (8 unit + 2 feature integration); fixtures `AuthorizedTransition`/`InstanceAuthorizedTransition`/`DeniedTransition`/`ThrowingTransition`/`AmbiguousTransition` cobrindo cada caminho. **Recovery notas:** A32 e B32 entregues auto. **C32** sub-agente parou sem testes/commit — orquestrador completou: ajustou teste Gate falhando (Laravel 12 `Gate::forUser(null)` com guest user requer Authenticatable real — substituiu user `null` por anonymous class implementing Authenticatable), corrigiu PHPStan via relaxar assinatura de `class-string` para `string` no método `authorize()` público (alternativa mais limpa que `class-string` cast em testes), atualizou SKILL com secção WF-006 + breaking change note, commit `feat(workflow): add TransitionAuthorizer with authorizeFor + Gate fallback`. **Total cumulative Fase 3:** 62 ai + 35 realtime + 39 workflow PHP + 17 workflow JS + 19 realtime JS = ~172 testes diretos.
- **Batch paralelo #31** (3 sub-agentes em worktrees, **3/3 entregues** — B31+C31 auto-completos, A31 recovery manual, merged 2026-04-30) — **Fase 3 progress slice 4**: AI-006 (`packages/ai/src/AiManager.php` final — front-door que resolve provider por nome (`config('arqel-ai.default_provider')` ou `options.provider`), aplica cache via `AiCache?->get`, enforça `CostTracker?->assertWithinLimit(Auth::id())`, persiste via `CostTracker?->record`, dispara `AiCompletionGenerated` event. `embed()` bypassa cache (vetores são leves de re-gerar) mas mantém limit assert. `chat()` cache key é `json_encode($messages)`); `CostTracker` final wrapper Eloquent sobre `ai_usage` — limites `null` ou `<=0` tratados como **ilimitado** (útil em dev), lança `DailyLimitExceeded`/`UserLimitExceeded` (subclasses de `AiException`, agora `class` não `final` para extensibilidade); `AiCache` final wrapper sobre `Cache::store()` — key determinística `arqel-ai:{md5(json_encode([prompt,options]))}`, TTL `arqel-ai.caching.ttl` (default 3600s), no-op quando `caching.enabled === false`; `Models/AiUsage` final Eloquent (tabela `ai_usage`, fillable `user_id/provider/model/input_tokens/output_tokens/cost_usd/prompt_hash`, casts adequados); migration `2026_05_01_000000_create_ai_usage_table` auto-discovered via `hasMigration` no provider; `Events/AiCompletionGenerated` final Dispatchable+SerializesModels com `result, providerName, userId`; `AiServiceProvider::packageRegistered()` instancia singleton `AiManager` resolvendo cada entry de `arqel-ai.providers` lazily via `$app->make($driver, $args)` (entries com `class_exists($driver) === false` silenciosamente skipadas); `tests/Fixtures/FakeProvider` para isolated AiManager testing; `TestCase::defineDatabaseMigrations()` adiciona `loadMigrationsFrom(__DIR__.'/../database/migrations')`; **27 Pest tests** novos (10 AiManager + 6 CostTracker + 3 AiCache + recovery dos 8 anteriores); total `arqel/ai` 51 testes verdes, PHPStan max clean). RT-009 (`packages/realtime/src/Channels/ResourceChannelAuthorizer.php` final readonly com 3 métodos estáticos `authorizeResource`/`authorizeRecord`/`authorizeActionJob` — defensive `try/catch \Throwable` + `Log::warning()`, resolve `ResourceRegistry` via FQCN-string + `app()->bound()` para evitar hard-dep cross-package; `routes/channels.php` ganhou 3 novos `Broadcast::channel()` patterns `arqel.{resource}` (Gate `viewAny`), `arqel.{resource}.{recordId}` (Gate `view`), `arqel.action.{jobId}` (Cache match no user id), todos delegando ao Authorizer; presence channel RT-004 preservado; **15 Pest tests** novos (10 unit + 5 feature) — total realtime 35 testes; SKILL.md ganhou secção "Channel authorization (RT-009)" com tabela dos 4 patterns + exemplo de Gate definition). WF-005 (`packages-js/workflow/src/WorkflowVisualizer.tsx` componente apresentacional puro **dependency-free** — função `buildMermaidSource(definition, currentState?, direction='LR'): string` gera string Mermaid (`graph LR\n` ou `TB`); FQCN slugify: `App\States\Pending` → node `Pending` (último segmento, sufixos `State`/`Status` removidos, colisões via `_2` suffix); `from: null` expande para edges saindo de cada state; `from: ['A','B']` array gera 2 edges; transitions sem label têm label inferido do FQCN basename; `currentState` aplica highlight `style {id} fill:#fbbf24,stroke:#d97706`; states com `color` CSS-like ganham `style {id} fill:{color}`; render default `<pre className="language-mermaid">{source}</pre>` ou via `renderer?: (src) => ReactNode` prop opcional para consumers que tenham mermaid runtime já configurado (~500KB evitado em peer); helper `slugifyStateId()` exportado; **9 vitest tests** novos cobrindo source generation/3 states+2 transitions/highlight current/direction TB/from-null any-state/from-array/custom renderer/buildMermaidSource pure; total `@arqel/workflow` 17 testes; SKILL.md secção "WorkflowVisualizer (WF-005)" PT-BR com exemplos render direto + custom renderer). **Recovery notas:** B31 e C31 entregues auto. **A31** sub-agente parou após criar AiCache + AiUsage + exceptions + migration mas sem AiManager/CostTracker/Event/AiServiceProvider update/testes — orquestrador completou: criou `AiManager` (~140 LOC), `CostTracker` (~85 LOC), `Events/AiCompletionGenerated`, atualizou `AiServiceProvider` com singleton resolution lazy, criou `FakeProvider` fixture + 3 test files (AiManagerTest 10/CostTrackerTest 6/AiCacheTest 3), corrigiu PHPStan tipando closure `function (Application $app)`, atualizou SKILL.md com secção AI-003..006. **Total cumulative Fase 3:** 51 ai + 35 realtime + 30 workflow PHP + 17 workflow JS + 4 realtime JS = 137 testes diretos.
- **Batch paralelo #30** (3 sub-agentes em worktrees, **3/3 entregues** — 2/3 auto-completos + C30 recovery manual, merged 2026-04-30) — **Fase 3 progress slice 3**: AI-005 (`packages/ai/src/Providers/OllamaProvider.php` final via `Http` facade — `__construct(baseUrl='http://localhost:11434', model='llama3.1', embeddingModel='nomic-embed-text')`; endpoints `POST /api/generate` + `/api/chat` + `/api/embeddings`; `chat()` faz `array_unshift` `{role:'system'}` quando system option presente; **cost SEMPRE 0.0** (asserted exato com 999.999 tokens); HTTP failure → `AiException`; **9 Pest tests**; total `arqel/ai` 32 testes; config entry `ollama` ganhou `base_url`/`model`/`embedding_model` envs `ARQEL_AI_OLLAMA_*`; **Decisão de compatibilidade**: mantido `'driver' => 'Arqel\\Ai\\Providers\\OllamaProvider'` FQCN ao invés de slug `'ollama'` para alinhamento com test contract já mergeado em AiServiceProviderTest). RT-008 (**novo pacote `packages-js/realtime/`** = `@arqel/realtime@0.10.0-rc.1` — package.json com **deps reais aprovadas pelo ticket**: `laravel-echo ^2.0.0` + `pusher-js ^8.4.0` (Reverb usa pusher protocol); peers `react ^19.2.0` + `@inertiajs/react ^3.0.0`; `src/setupEcho.ts` exporta função `setupEcho(config)` SSR-safe (`typeof window === 'undefined'` no-op + warn) + idempotente (warn + return se `window.Echo` já existir), instancia `new Echo({broadcaster:'reverb', ...config})` e attacha `window.Pusher`; `src/types.ts` exporta `EchoConfig`/`EchoLike`/`EchoChannelLike`/`PresenceChannelLike`/`PresenceMember`/`EchoEventListener`/`EchoConnectorLike` sem redeclarar `Window`; `src/index.ts` re-exporta apenas `setupEcho` + tipos — **NÃO** importa hooks de `@arqel/hooks` (preserva code-splitting); **4 vitest tests** com `vi.hoisted` + `vi.mock('laravel-echo')` + `vi.mock('pusher-js')` cobrindo happy path/config pass-through/idempotência/SSR-safe; build clean ESM+DTS). WF-004 (`packages/workflow/src/Events/StateTransitioned.php` final com `Dispatchable` + `SerializesModels`, public readonly `record: Model, from: string, to: string, userId: ?int, context: array<string,mixed>` — **NÃO** implementa `ShouldBroadcast` por design (broadcasting opt-in via listener para manter pacote standalone); `Concerns/RecordsStateTransition` trait opcional para classes spatie `Transition` que prefiram disparar evento canônico no próprio `handle()`; **`HasWorkflow::transitionTo(string $newState, array $context = [])`** helper que captura state atual via `resolveStateKey`, delega à API spatie quando `state->transitionTo()` existir ou faz attribute assignment + save direto; após mutação dispara `StateTransitioned` capturando `Auth::id()`; suprime evento quando `arqel-workflow.audit.enabled === false` ou `audit.log_via !== 'event'`; config bloco `audit{enabled, log_via}` + `broadcast_transitions` flag; **8 Pest tests** novos cobrindo dispatch/context propagation/Auth::id()/null userId/audit-disabled skip/log_via-silent skip/persistência/trait fixture; total workflow 30 testes; PHPStan max clean após `assert($this instanceof Model)` + `@phpstan-require-extends Model` no trait; SKILL.md ganhou secção "Entregue (WF-004)"). **Recovery notas:** A30 e B30 entregues auto. **C30** sub-agente parou no meio (criou Events + trait + transitionTo helper mas não escreveu testes nem commitou) — orquestrador completou: adicionou config audit+broadcast, criou `StateTransitionedEventTest` com 8 testes, criou `SampleSpatieTransition` fixture para exercitar trait (PHPStan reclamava trait usado zero vezes), corrigiu erros PHPStan (`assert instanceof Model` + `array<string,mixed>` doc no fixture), Pint format, SKILL update, commit `feat(workflow): add StateTransitioned event + transitionTo audit helper`. **Total cumulative Fase 3:** 32 ai + 20 realtime + 30 workflow PHP + 8 workflow JS + 4 realtime JS = 94 testes diretos.
- **Batch paralelo #29** (3 sub-agentes em worktrees, **3/3 entregues auto-completos**, merged 2026-04-30) — **Fase 3 progress slice 2**: AI-004 (`packages/ai/src/Providers/OpenAiProvider.php` final via `Http` facade — endpoints `https://api.openai.com/v1/chat/completions` + `/embeddings`, header `Authorization: Bearer`; modelo default `gpt-4o-mini`; `chat()` faz `array_unshift` da mensagem `system` (vs campo separado do Claude); suporta `temperature`/`max_tokens`/`response_format` + atalho `json_mode => true` → `{type:'json_object'}`; `embed()` hardcoded em `text-embedding-3-small` (1536d) com TODO `AI-004-large`; pricing constants $0.15/$0.60 MTok; `stream()` lança AiException com TODO `AI-004-stream`; HTTP failure → `AiException` com body+status; **8 Pest tests** cobrindo complete+cost/system prepend/JSON mode/embedding 1536d/HTTP 401/stream throw/capability flags/options override; total `arqel/ai` 23 testes). RT-004 PHP slice (`packages/realtime/routes/channels.php` registando `Broadcast::channel('arqel.presence.{resource}.{recordId}', ...)` retornando `{id, name, avatar}` para autenticado, `false` se Gate `view-resource-presence` registrada e nega; `Presence/PresenceChannelResolver` final readonly com `forResource(slug, recordId)` → `arqel.presence.{slug}.{recordId}`, lendo pattern de config (`strtr` placeholders), lança `RealtimeException` se `presence.enabled === false`; `Exceptions/RealtimeException` final extends RuntimeException; `RealtimeServiceProvider::configurePackage()` ganhou `->hasRoute('channels')` + `packageBooted()` chama `Broadcast::routes()` (idempotente Laravel-side); config bloco `presence{enabled, channel_pattern}`; SKILL.md secção "Presence channels (RT-004)"; **9 Pest tests** novos (5 unit + 4 feature) — total realtime 20 testes; React `useResourcePresence` hook deferido para batch futuro). WF-003 React slice (**novo pacote `packages-js/workflow/`** = `@arqel/workflow@0.10.0-rc.1` — package.json/tsconfig/tsup/vitest mirroring fields-advanced; `src/StateTransition.tsx` componente apresentacional puro: pill grande com `currentState.label` (cor de fundo via `currentState.color` ou fallback gray-200); botões para `transitions.filter(authorized)` habilitados, não-autorizadas mostradas disabled quando `showDescription=true`; click dispara `CustomEvent('arqel:state-transition', { detail: {from, to, name, recordId} })` no document OU chama `onTransition?(from, to)` se prop presente (NÃO faz fetch direto — apresentational); timeline `<ol>` com history items quando `showHistory && history.length`; vazio: "No state assigned." / "No transitions available."; `src/register.tsx` adapter `FieldRendererProps`→`StateTransitionProps` registrando lazy sob `'arqel/workflow/StateTransition'`; **8 vitest tests** cobrindo pill/transitions filter/click event/history timeline/null state/empty transitions/onTransition override; ambient.d.ts shims para `@arqel/ui/form`+`@arqel/types/fields` por causa de bug pré-existente em DTS build de fields-advanced — fora do escopo). **Total Phase 3 cumulative:** 23 ai + 20 realtime + 22 workflow PHP + 8 workflow JS = 73 testes diretos + scaffolds. PHPStan max clean, biome clean, Pint clean.
- **Batch paralelo #28** (3 sub-agentes em worktrees, **3/3 entregues auto-completos**, merged 2026-04-30) — **Fase 3 progress slice 1**: AI-003 (`packages/ai/src/Providers/ClaudeProvider.php` final implementing `AiProvider` via `Illuminate\Support\Facades\Http` — endpoint `https://api.anthropic.com/v1/messages` com header `anthropic-version: 2023-06-01`, modelo default `claude-opus-4-7`, max_tokens 4096; cost tracking via constantes pricing Opus 4.7 ($15/MTok input + $75/MTok output, round 6 decimals); `complete()`/`chat()` (system+temperature suportados; concatena múltiplos blocks `text` ignorando `tool_use`); `embed()` e `stream()` lançam `AiException` (stream marcado `// AI-003-stream follow-up` para SSE consumer real); HTTP failure → `AiException` com body+status; `name()='claude'`, `supportsStreaming()=true`, `supportsEmbeddings()=false`; **9 Pest tests** com `Http::fake()` cobrindo happy path/system+temperature/header omission/HTTP 401/embed throw/stream throw/capabilities/multi-text concat/options override). RT-003 (`packages-js/hooks/src/useResourceUpdates.ts` — hook React subscreve `arqel.{slug}` ou `arqel.{slug}.{id}` via `window.Echo.private().listen()`; **SSR-safe** com `typeof window !== 'undefined'` guard; **no-op silencioso** quando `Echo` ausente; `recordId === 0` tratado corretamente; cleanup via `Echo.leave()`; chama `router.reload({ only: recordId ? ['record'] : ['records'] })` via `@inertiajs/react`; **sem dep `laravel-echo`** — apenas tipos ambient `EchoLike`/`EchoChannelLike`; callback `onUpdate?(payload)` opcional; custom event name option; **8 vitest tests** com `vi.mock` de `@inertiajs/react` cobrindo no-op/canal sem+com recordId/reload only-keys/onUpdate/cleanup/custom event; subpath export `./useResourceUpdates` em package.json + tsup entry). WF-003 PHP slice (`packages/workflow/src/Fields/StateTransitionField.php` — `final class` extending `Arqel\Fields\Field`; setters fluent `showDescription/showHistory/transitionsAttribute/record`; `getTypeSpecificProps()` retorna `currentState/transitions/history/showDescription/showHistory/transitionsAttribute`; `resolveAvailableTransitions()` introspeciona transition classes via `ReflectionMethod` (estáticas `from()`/`to()`), filtra pelo state atual e checa `Gate::allows("transition-{from}-to-{to}", $record)` se Gate root disponível; `resolveCurrentState()` lê `$record->{$definition->getField()}`, normaliza objeto/BackedEnum/string e cruza com `WorkflowDefinition::getStateMetadata()`; `resolveHistory()` retorna `[]` com TODO `WF-007`; component string `'arqel/workflow/StateTransition'` (React component fica para C29); **7 Pest tests** cobrindo make()/type-component/flag propagation/no-record null/fixture mirror/JSON-encodability/transitionsAttribute. **Adicionado `arqel/fields: "@dev"` ao `packages/workflow/composer.json` `require` + path-repo `../fields` symlink** seguindo padrão dos outros pacotes). **Total Phase 3:** 22 wf + 15 ai + 38 hooks (cumulative novos do batch: 7+9+8 = 24 testes diretos do ticket). PHPStan max clean em PHP, biome lint clean em JS, Pint clean. **Recovery:** workflow composer.lock root precisou `composer update arqel/fields` após merge (path-repo new entry).
- **Batch paralelo #27** (3 sub-agentes em worktrees, **3/3 entregues**, merged 2026-04-30) — **Arranque da Fase 3**: AI-001/AI-002 (`packages/ai` scaffold — `AiServiceProvider` via spatie PackageServiceProvider; `config/arqel-ai.php` com `default_provider`/`providers` map (Claude/OpenAI/Ollama FQCNs como string-literals, eager-resolution evitada para AI-003+)/`cost_tracking{daily_limit_usd, per_user_limit_usd}`/`caching{ttl: 3600}`; `Contracts\AiProvider` interface (complete/chat/embed/stream/name/supportsEmbeddings/supportsStreaming); `AiCompletionResult` `final readonly class` com `totalTokens()` helper; `Exceptions\AiException` final; SDKs Anthropic/OpenAI/Ollama em `suggest`; 6 Pest tests). RT-001/RT-002 (`packages/realtime` scaffold — `RealtimeServiceProvider` + config (`channel_prefix=arqel`, `auto_dispatch.resource_updated=true`); `Events\ResourceUpdated implements ShouldBroadcast` com **dois** PrivateChannels `arqel.{slug}` + `arqel.{slug}.{id}` (per-record skip se `getKey()===null`), slug resolution defensiva via `Resource::getSlug()` com fallback kebab-cased basename catching Throwable; `Concerns\BroadcastsResourceUpdates` trait gated por config flag, captura `Auth::id()` quando autenticado; suggest `laravel/reverb`; 11 Pest tests). WF-001/WF-002 (`packages/workflow` scaffold — `WorkflowServiceProvider` + `config/arqel-workflow.php`; `WorkflowDefinition` PHP-first DSL declarando states + transitions; `Concerns\HasWorkflow` trait integrando `spatie/laravel-model-states`; 15 Pest tests com fixtures `States/Pending→Paid→Shipped`/`Cancelled` cobrindo guards e transition registration). **commitlint scopes:** já tinha `realtime`+`workflow`, scope `ai` adicionado. **Composer root require-dev:** `arqel/ai`, `arqel/realtime`, `arqel/workflow` adicionados em ordem alfabética. **Total Phase-3 startup:** 32 testes verdes (6 ai + 11 realtime + 15 workflow), PHPStan max clean, Pint formatado em todos os 3 pacotes. **Recovery notas:** A27 (ai) — sub-agente bloqueado em Bash, fechado manualmente. B27 (realtime) — sub-agente esqueceu `composer.json::repositories` path-repo + commit; recuperado manualmente. C27 (workflow) — entregue auto (commit `e425571`).
- **Batch paralelo #26** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): FIELDS-ADV-010 full (`RichTextInput.tsx` upgrade contentEditable→Tiptap v2 — `useEditor` com StarterKit + Link + Image + Placeholder + CharacterCount; 13 toolbar buttons via `editor.chain().toggleX().run()`; image upload multipart POST → `editor.commands.setImage`; safeUrl guard preserva rejeição de `javascript:`/`data:`/`vbscript:`; deps `@tiptap/{react,starter-kit,extension-link,extension-image,extension-placeholder,extension-character-count} ^2.10.0`), FIELDS-ADV-012 full (`CodeInput.tsx` Shiki overlay — `useShikiHtml(value, language, theme)` hook lazy-loads via `import('shiki')`, transparent textarea sobre `<pre>` opaco com sync scroll; theme `'github-dark'` default + auto-switch `'github-light'` via `matchMedia('(prefers-color-scheme: light)')`; plaintext + unknown languages skip Shiki; dep `shiki ^3.0.0`), FIELDS-ADV-013/014 full (`RepeaterInput.tsx` + `BuilderInput.tsx` dnd-kit — `<DndContext>` + `<SortableContext>` (vertical strategy) + `<SortableItem>` wrapper com `useSortable`; drag handle `≡` na esquerda como único listener de drag; up/down icon buttons preservados como keyboard fallback; PointerSensor 5px activation + KeyboardSensor; deps `@dnd-kit/{core ^6.1.0,sortable ^8.0.0,utilities ^3.2.0}`). **114 testes total `@arqel/fields-advanced` (was 107, +7). Fase 2 100% completa — todos os deferred React component upgrades agora shipped.**
- **Batch paralelo #25** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): FIELDS-ADV-014 scoped (`BuilderInput.tsx` heterogeneous block list — sem dnd-kit; custom dropdown picker `role=menu` + ESC + backdrop close em vez de Base UI Dialog; per-block schema resolution → SubFieldInput; itemLabel template + collapsible items; output `[{type, data}]` no `__id`; 14 vitest tests), FIELDS-ADV-017 scoped (`WizardInput.tsx` multi-step — sem React 19.2 `<Activity>`; progress bar com `aria-current="step"`; per-step validação required-field; `persistInUrl=true` sync `window.location.hash`; `wizard:submit` event dispatched no last step; live region polite; 12 vitest tests), Cross-package surface smoke (extended `tests/index.test.tsx` com 2 testes — todos os 8 lazy slots resolvem + dynamic-import sem throw; `widgets-surface.test.ts` + `palette/surface.test.ts` no `@arqel/ui` validam type re-exports; SKILL Coverage subsections updated). **107 fields-advanced tests · 126 ui tests. Fase 2 essencialmente completa — todos os React components shipped (scoped). Tiptap full / Shiki highlighting / dnd-kit drag deferidos.**
- **Batch paralelo #24** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): FIELDS-ADV-010 scoped (`RichTextInput.tsx` contentEditable-based — sem Tiptap dep; pure-DOM `sanitizeHtml` com tag/attr allowlist + `javascript:`/`data:`/`vbscript:` URL guard + strip script/style/iframe; 13 toolbar buttons gated por `field.props.toolbar` — bold/italic/underline/strike/h1-3/ul/ol/blockquote/link/code/image; image upload via multipart POST a `imageUploadRoute`; maxLength text-content tracking com warning + hard revert at +10%; A11y role=textbox + role=toolbar; 18 tests inc. XSS guard), FIELDS-ADV-012 scoped (`CodeInput.tsx` monospace textarea — sem CodeMirror/Shiki; line-numbers gutter com sync scroll; Tab/Shift+Tab insert/remove indent; configurable tabSize/wordWrap/minHeight; fullscreen toggle; language badge; 11 tests), FIELDS-ADV-013 scoped (`RepeaterInput.tsx` lista sub-form items com up/down buttons — sem dnd-kit; generic SubFieldInput renderer text/number/select/textarea/boolean; add/remove/clone com min/max enforcement; collapsible items; itemLabel template `{{key}}`; A11y fieldset/legend/article + aria-labels; 13 tests). **79 testes total `@arqel/fields-advanced` (was 25, +54). Tiptap (full RichText), Shiki (CodeInput highlighting), dnd-kit (Repeater drag) deferidos para follow-ups.**
- **Batch paralelo #23** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): WIDGETS-010 part 1 (`WidgetWrapper.tsx` chrome com loading skeleton + error+retry + aria-label section + `StatCard.tsx` KPI com inline-SVG sparkline (sem Recharts) + 6-color palette + optional url wrap; 15 testes), WIDGETS-010 part 2 (`ChartCard.tsx` polymorphic dispatcher 6 chart types via React.lazy + Suspense — keeps Recharts 60KB out of main chunk; sub-componentes `Line/Bar/Area/Pie/Donut/Radar` em `charts/`; recharts ^2.15.0 em peer+devDeps; chartData translation labels+datasets→Recharts row shape; 7 testes), WIDGETS-010 part 3 + WIDGETS-011 (`TableCard.tsx` minimal `<table>` + see-all + loadError alert; `DashboardFilters.tsx` select+date_range com onChange; `WidgetRenderer.tsx` dispatch by type com polling via setInterval+cleanup + deferred fetch on mount + optional fetcher prop; `DashboardGrid.tsx` header+filters+responsive CSS Grid; 22 testes). **122 testes total `@arqel/ui` (was 92, +30); ./widgets export wired em tsup + package.json.**
- **Batch paralelo #22** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): FIELDS-ADV-011 scoped (`MarkdownInput.tsx` textarea-based — tiny dependency-free `markdownToHtml()` cobrindo headings/bold/italic/inline code/code blocks/links/lists/paragraphs com HTML escape + javascript:/data:/vbscript: rejeitados; 3 modos `side-by-side`/`tab` com `role=tab`/`popup` com `<dialog>`; toolbar 6 buttons; fullscreen toggle; rows clamp ≥ 3; a11y semântica; 12 vitest tests inc. XSS guard. **Full CodeMirror+remark deferred** — pure React + Tailwind), CMDPAL-003 (`<CommandPalette />` em `@arqel/ui` — HTML `<dialog>` nativo sem Base UI Dialog dep; Cmd+K/Ctrl+K global window listener; 150ms-debounced fetch a `/admin/commands?q=`; results grouped by category em listbox; ArrowUp/Down + Enter via aria-activedescendant; localStorage recents top 5/30-day window; click backdrop/Escape closes; aria-live count; mounted once em AppShell em todas as 4 variants; 8 vitest tests; export `./palette` + tsup entry; 78 testes total `@arqel/ui`), CMDPAL-004 (`CommandRegistry::registerStatic()` sugar com duplicate-id throw + `registerClosureProvider()` sugar; `Command` ganhou `?bool $requiresAuth` + `?bool $hideForAuthenticated` flags filtradas em `resolveFor()` antes do fuzzy rank; trait `Concerns\HasCustomCommands` para user-land providers; 8 unit tests + SKILL example; 163 testes total core).
- **Batch paralelo #21** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): FIELDS-ADV-018 scoped (`@arqel/fields-advanced` JS package scaffold em `packages-js/fields-advanced/` — package.json/tsconfig/tsup/vitest mirroring @arqel/fields; `src/register.ts` com 8 lazy `registerField()` calls via `React.lazy(() => import())` para code-splitting; 8 stub components `<Name>Input.tsx` cada um wrapping shared `PlaceholderInput` que emite hidden input para round-trip de value; SKILL.md PT-BR canonical), FIELDS-ADV-015 (`KeyValueInput.tsx` real — pure React, defensive prop narrowing dos 9 props PHP; state interno `Array<{__id, key, value, __justAdded}>` com hydrate from value/object switching; `useEffect` re-hydrate só se diff JSON; output array OR object via `asObject`; a11y fieldset/legend/aria-label/aria-invalid; 9 vitest tests), FIELDS-ADV-016 (`TagsInput.tsx` real — pure React combobox; Enter/separator commit, Backspace remove last, Escape close, ArrowUp/Down navigate; `unique` case-insensitive dedup, `maxTags` enforce, `creatable=false` rejeita unknown; suggestions filter case-insensitive; chips com remove button; a11y combobox/listbox/option roles + per-chip aria-label; 15 vitest tests). **25/25 testes verdes em @arqel/fields-advanced (1 smoke + 9 KeyValue + 15 Tags); merge resolveu conflitos cruzados — A21 scaffold canônico, B21+C21 components reais sobrepondo stubs.** **vitest.config.ts atualizado para incluir `src/**/*.test.tsx`.**
- **Batch paralelo #20** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): DOCS-V2-001 scoped (6 overview guides em `docs/guides/` — tenancy/dashboards/advanced-fields/mcp/tables-v2/command-palette + README index; ~945 linhas markdown sourced verbatim do SKILL.md polido de cada package), DOCS-V2-002 scoped (`docs/migrations/` com `from-filament.md` 12 side-by-side patterns + matriz 18 features + checklist + 5 pitfalls; `from-nova.md` 12 patterns adaptados Lenses/Cards/Metrics/Tools + matriz 19 features; `README.md` decision tree), GOV-V2-001 scoped (`docs/release-checklist-v0.8.md` 11-section checklist com pre-flight/version bumps/changelog cut/signed tag/splitsh/Packagist/npm publish/smoke test/announce/rollback; 5 npm `package.json` bumped 0.0.0→0.8.0-rc.1; CHANGELOG reorganizado com `[Unreleased]` placeholder + `[0.8.0-rc.1 — pendente de tag]` cobrindo Batch #1..#19). **Marca operacional**: monorepo agora pronto para tag manual `v0.8.0-rc.1` seguindo o checklist; release pipeline ainda manual (splitsh + npm publish via release.yml).
- **Batch paralelo #19** (3 sub-agentes, merged 2026-04-30): FIELDS-ADV-019 PHP slice (6 cross-field coverage tests em `tests/Unit/Coverage/CrossFieldCoverageTest.php` — invariants partilhados entre os 8 field types: every extends Field, getTypeSpecificProps array non-null, type/component non-empty strings, payload JSON-encodable (catches resource/closure leaks), make() returns same-class with correct name, RichText imageUploadDisk verbatim propagation; 130 testes total fields-advanced), FIELDS-ADV-020 (SKILL.md restruturado canonical layout — 9 verbose entries → 5 grouped feature areas Setup/Rich content/Dynamic structure/Map-list/Multi-step + Coverage + Por chegar; 4 examples; preservou conventions + anti-patterns críticos), MCP-009 scoped (`McpServer::serveStreams(input, output)` testable loop helper extraído de `serve()`; `McpConversationTest` simula full Claude Desktop conversations via `php://memory` — initialize→tools/list→tools/call, resources/read, prompts/get, malformed input handling; 93 testes total mcp). **2/3 entregues auto (B19+A19); C19 stopped before commit, fechado manualmente.**
- **Batch paralelo #18** (3 sub-agentes, merged 2026-04-30): WIDGETS-014 scoped (7 testes coverage gaps em `tests/Unit/Coverage/CoverageGapsTest.php` — WidgetRegistry overwrite vs DashboardRegistry throw asymmetry, register rejects non-existent class-strings, Dashboard.filters mode reset, partial widget filter merge per-key, addWidget appends, filterValue default fallback, resolve empty when no entries; 146 testes total widgets), WIDGETS-015 (SKILL.md restruturado canonical layout — Purpose/Status grouped/Conventions/Anti-patterns/Examples/Related, 3 usage examples), TENANT-015 (SKILL.md restruturado canonical — 9 grouped feature areas, anti-patterns + cross-tenant leakage checklist 8 pontos preventivos, 3 examples). **Apenas A18 e C18 entregues auto; B18 sandbox bloqueou Bash, fechado manualmente.**
- **Batch paralelo #17** (3 sub-agentes, **3/3 entregues auto-completos**, merged 2026-04-30): TABLE-V2-008 PHP slice (`Table::paginationType(string)` com 4 constantes `PAGINATION_LENGTH_AWARE`/`PAGINATION_SIMPLE`/`PAGINATION_CURSOR`/`PAGINATION_INFINITE`, default `'lengthAware'`, defensive fallback; toArray emite key dentro de `config{}`; 9 testes), TABLE-V2-009 scoped (7 testes integration cross-feature em `tests/Feature/TableV2IntegrationTest.php` exercitando V2-002..008 em combinação: toArray emite all V2 keys; togglable+grouping; QueryBuilderFilter co-existe com filtros regulares; inline editing columns; grouping+reorderable+summaries combo; mobileMode preserva config; per-column hiddenOnMobile flag), TABLE-V2-010 scoped (SKILL.md restruturado para canonical layout — Purpose/Status/Conventions/Anti-patterns/Examples/Related, 8 secções V2 consolidadas em Status block grouped). **170 testes total table.**
- **Batch paralelo #16** (3 sub-agentes, merged 2026-04-30): TABLE-V2-006 PHP slice (`Table::reorderable(?string $columnName = 'position')` setter — null disables, isReorderable() + getReorderColumn(), toArray() expõe key; 6 testes), TABLE-V2-007 PHP slice (`Table::mobileMode(string)` com constants `MOBILE_MODE_STACKED`/`MOBILE_MODE_SCROLL`, default 'stacked', defensive fallback para unknown; 7 testes — 154 table total), CMDPAL-005 scoped (6 testes coverage gaps: ordering invariant static-before-provider, FuzzyMatcher tie-breaking insertion order preserved, rank limit=0, Command toArray shape, NavigationCommandProvider broken pluralLabel skip; SKILL.md polish para canonical CMDPAL-001/002 entries; 154 core total). **Apenas B16 entregou auto; A16 sandbox blocked Bash, C16 entregue. Orquestrador finalizou A16+C16 manualmente.**
- **Batch paralelo #15** (3 sub-agentes, merged 2026-04-30): TABLE-V2-004 PHP slice (`Column::togglable()` / `hiddenByDefault()` (auto-enables togglable) / `hiddenOnMobile()` flags fluentes; `toArray()` expõe 3 keys; 9 testes — 65 table), TABLE-V2-005 PHP slice (`Summary` abstract base + 5 concretos Sum/Avg/Count/Min/Max + static facade `Summary::sum/avg/count/min/max`; `Table::groupBy(field, ?labelResolver)` + `groupSummaries(array)` + `buildGroups(Collection): array<{label, key, records, summaries}>`; toArray() expõe groupBy+summaries; 16 testes — 141 table), MCP-010 (SKILL.md restruturado para canonical layout com Setup subsection + Claude Desktop config snippet; 4 testes coverage gaps; 87 mcp). **Apenas A15 entregue auto; B15 timed out (Stream idle 4500s); C15 falhou (stream watchdog 600s). Orquestrador completou B15+C15 manualmente.**
- **Batch paralelo #14** (3 sub-agentes, merged 2026-04-30): TABLE-V2-003 PHP slice (`Constraint` abstract + 5 concretes Text/Number/Date/Boolean/Select com operators per type, `apply($query, $op, $value, $method)` + `toArray()` para React; `QueryBuilderFilter::applyToQuery` recursivo com AND/OR groups + nested groups; **field whitelisting via `findConstraint` é SECURITY** — unknown fields silently dropped; Number/Date validation defensiva; 35 testes → 117 table), CMDPAL-002 scoped (`NavigationCommandProvider` itera ResourceRegistry com defensiva try/catch + `ThemeCommandProvider` 3 commands estáticos light/dark/system; auto-register em `packageBooted`; 10 unit + 2 feature → 148 core; **CreateCommandProvider/RecordSearchProvider deferidos** — precisam Policy + Resource model traversal), AUDIT-004 scoped (5 testes coverage gaps em `tests/Unit/Coverage/`: LogOptions canonical 4-defaults verification + log name basename; stringAttr causer serialisation tolerance — id-only/missing attribute/non-string; 36 total audit; **E2E UI tests + retention pruning command deferidos**). **Apenas A14 entregue auto-completo; B14 mid-lint-fix abortou; C14 abortou — sandbox bloqueou Bash. Orquestrador completou B14+C14 manualmente.**
- **Batch paralelo #13** (3 sub-agentes, merged 2026-04-30): TABLE-V2-002 PHP slice (`TextInputColumn`/`SelectColumn`/`ToggleColumn` final extends Column — `editable=true` default, `rules()`/`debounce(int)` clamp ≥0/`readonly(bool|Closure)` setters; SelectColumn aceita Closure options resolved lazy; ToggleColumn `onValue/offValue` mixed; 22 testes), CMDPAL-001 scoped (`Arqel\Core\CommandPalette\` namespace — `Command` readonly value-object, `CommandProvider` contract, `CommandRegistry` singleton com static + lazy providers, `FuzzyMatcher::score` 100/95/80/50/0 + `rank` com limit, `CommandPaletteController` em `GET /admin/commands` + 138 testes core. **Built-in providers Navigation/Create/RecordSearch/Theme deferidos para CMDPAL-002**), TENANT-013 scoped (`HasFeatures` trait + `RequireTenantFeature` middleware com alias `arqel.tenant.feature` (404/500/402/next), `ScaffoldBillingCommand` (`arqel:tenant:scaffold-billing` com 3 stubs), `laravel/cashier-stripe` em `suggest:` — apps opt-in; 17 testes → 157 total tenant. **Cashier wiring deferido** — consumer customiza). **Apenas A13 entregue auto; B13/C13 escreveram impl mas sandbox bloqueou commit, fechados manualmente pelo orquestrador.**
- **Batch paralelo #12** (3 sub-agentes, merged 2026-04-29): EXPORT-006 scoped (`ProcessExportJob` queueable + `RecordsResolver` contract + `ExportLogger` contract + `NullExportLogger` default + `ExportDownloadController` UUID-glob lookup + rota `GET /admin/exports/{id}/download`; 11 testes), TENANT-012 scoped (`Theming\TenantTheme` readonly value-object + `TenantThemeResolver` singleton + `CssVarsRenderer` static helper com defesa anti-injeção HTML; 18 testes), TENANT-014 scoped (3 gaps cobertura: `runFor` exception path / middleware MODE_OPTIONAL / `AbstractTenantResolver::availableFor` defaults; 9 testes). Tenant 140 testes total. **A12 + C12 abortaram (sandbox blocked Bash); orquestrador implementou manualmente. Apenas B12 entregue auto.**
- **2ª rodada bug fixes dogfooding** (commits `685427a`, `4995891`, `0de7869`): script setup-test-app: ARQEL_PHP_PACKAGES list extended +5 (widgets/fields-advanced/mcp/audit/export); APP_TSX_MARKER bumped para detectar legacy template e re-escrever; cleanup de `Pages/{arqel,Arqel}` legacy; +5 peer deps (tailwindcss, @base-ui-components/react, @tanstack/react-table, lucide-react, @tailwindcss/vite); vendor:publish activitylog migrations; cleanup de `public/hot` stale (Vite dev URL morto bloqueava boot React); login route stub para evitar 500 `Route [login] not defined`. **Root cause do "panels not mounting": `tailwindcss` faltava em `@arqel/ui` devDeps — `@import 'tailwindcss'` em globals.css falhava resolver. Adicionado em devDependencies.**
- **Batch paralelo #11** (3 sub-agentes, merged 2026-04-29): WIDGETS-013 (`MakeWidgetCommand` + `MakeDashboardCommand` artisan; 4 widget stubs (stat/chart/table/custom) + dashboard stub; snake-case derivation auto; idempotent; 8 feature tests = 139 widgets total. **A11 abortou — sandbox blocked Bash; orquestrador implementou manualmente**), FIELDS-ADV-009 (`WizardField` + `Step` value-object — final classes; `steps()` filtra non-Step; **duplicate step name throws InvalidArgumentException**; `persistInUrl` + `skippable` toggles; `Step::label()` fallback humanised; macro `wizard` 8º; 5 Step + 12 Wizard + 1 provider tests = 124 fields-advanced), AUDIT-003 scoped (`GlobalActivityLogController::index` paginator com 8 query params: `log_name`/`event` whitelist/`causer_type`+`id`/`from`+`to` ISO dates/`per_page` clamp 1..200; whitelist enforce 400 + Carbon parse defensive 400; `config/audit.php` com `global_log_url` + nav hints; rota `GET /admin/audit/activity`; 11 + 2 testes = 31 audit. **Resource integration deferred (cross-package).**). **B11 e C11 entregues auto-completos**.
- **Batch paralelo #10** (3 sub-agentes, merged 2026-04-29 — **2º batch com 3/3 entregues auto-completos**): FIELDS-ADV-008 (`TagsField` final extends Field — suggestions/creatable/maxTags/separator setters; `unique` payload key preservado mas setter renomeado para `uniqueTags()` para não quebrar LSP do `Field::unique()` herdado de `HasValidation`; suggestions aceita Closure resolved lazy; macro `tags` → 7º; 14 unit + 1 provider tests = 106 total), TENANT-011 (`arqel:tenant:scaffold-profile` artisan command — gera `TenantSettingsController` + rotas `/admin/settings/tenant` + `TenantSettings.tsx` stub; idempotente; mirror exato do TENANT-010; 5 feature tests = 113 total), MCP-008 (`MigrateFilamentResourcePrompt` + `ReviewResourcePrompt` em `Prompts/`; closure file reader injetável; path traversal `..` bloqueado antes da invocação do reader; auto-registo em `packageBooted`; provider closure forwards apenas `messages` array porque McpServer já wrapa `{description, messages}`; 13 unit + 3 feature → 83 testes mcp).
- **Batch paralelo #9** (3 sub-agentes, merged 2026-04-29): MCP-007 (`SkillResource` expõe `arqel-skill://<package>` URIs apontando para SKILL.md de cada package; closure injection para testes; path resolution dual-mode com fallback `__DIR__/../../../..` para dogfooding monorepo; auto-registo pre-flattened — 1 resource McpServer por SKILL descoberto; 11 testes), FIELDS-ADV-007 (`KeyValueField` final extends Field — 9 props: keyLabel/valueLabel não-vazios obrigatórios, placeholders aceitam vazio, editableKeys/addable/deletable/reorderable toggles, `asObject(bool)` switch para output shape `[{key,value}]` vs `{key: value}`; macro `keyValue`; 14 unit + 1 provider tests = 91 total), EXPORT-005 scoped (`ExportAction::execute()` real — resolve format → exporter → file write em `withDestinationDir()`; `withColumns()` + `dryRun()` setters fluentes; retorna `{path, filename, format, mimeType}`; 9 unit tests com XLSX/PDF skip por ext-zip/dompdf. **Form modal + queue threshold + ProcessExportJob + flash notification deferred para EXPORT-006**). Testes: mcp 67, fields-advanced 91, export 41 (34 passed + 7 skip). **A9 e C9 entregues auto-completos**; B9 implementação completa mas sandbox blocked git, fechado manualmente.
- **Batch paralelo #8** (3 sub-agentes, merged 2026-04-29): MCP-006 (`RunTestTool` Pest/PHPUnit wrapper via Symfony Process; Closure runner injetável; **path traversal `..` e absolute path bloqueados** com `InvalidArgumentException`; timeout clamp `[1, 600]`; auto-registo como 4ª tool; 10 unit + 1 feature → 56 testes mcp), TENANT-010 (`arqel:tenant:scaffold-registration` artisan command — gera `TenantRegistrationController` + `routes/tenant-registration.php` snippet + `TenantRegister.tsx.stub`; idempotente — re-run sem `--force` skip files; honra `arqel.tenancy.model` config; `setBasePath()` testbench helper; 5 feature tests → 108 testes tenant), FIELDS-ADV-006 (`Block` abstract base com `type/label/icon/schema` + `BuilderField` heterogéneo com `blocks()` aceitando 3 modos — class-string list, assoc map, ou misto; **duplicate `type()` lança InvalidArgumentException**; mesmas invariantes min/max do RepeaterField; macro `builder` → 76 testes fields-advanced). **A8 e C8 entregues auto-completos**; B8 implementação completa mas sandbox blocked git commit, fechado manualmente pelo orquestrador.
- **Batch paralelo #7** (3 sub-agentes, merged 2026-04-29 — **primeiro batch com 3/3 entregues auto-completos**): WIDGETS-009 (`Filter` abstract base + `DateRangeFilter` + `SelectFilter` em `Filters/` namespace; `Widget::filterValue` + `getFilters` readers; `Dashboard::filters` dual-mode legacy `array<string,mixed>` ou novo `list<Filter>`; getters `getDeclaredFilters`/`getFilterDefaults`; propagação via `array_merge($dashboardDefaults, $widget->getFilters())` em `resolve()` — request-time vence; 15 testes novos), EXPORT-004 (`PdfExporter` real via dompdf/dompdf hard dep; HTML table render + escaping; `setOrientation`/`setPaperSize` setters; `streamDownload` static helper; 8 testes guardados por `Dompdf\Dompdf` + `ext-mbstring`), AUDIT-002 backend (`RecordActivityController::show($subjectType, $subjectId)` retornando paginator Laravel; rota `GET /admin/audit/{subjectType}/{subjectId}/activity` com regex `where('.*')` para FQCN; `LogsActivity::activityLog($perPage)` trait helper; 7 testes). Testes: widgets 131, export 30 (24 passed + 6 skip ext-zip), audit 18. **React `ActivityTimeline` + Resource-base `showActivityLog` integration deferred** (cross-package work).
- **Batch paralelo #6** (3 sub-agentes, merged 2026-04-29): MCP-005 (`GenerateResourceTool` wrapper para `arqel:resource` artisan command — Closure runner para testabilidade; auto-registo como 3ª tool em `packageBooted`; payload `{model, exitCode, output, success}`. **Divergence note**: real MakeResourceCommand não tem `--from-model` flag — schema MCP mantém `fromModel` por forward-compat mas runner default strip-out o flag), EXPORT-003 (`XlsxExporter` real via spatie/simple-excel — `formatCell` preserva DateTime nativo para Excel render; `streamDownload` HTTP helper; 6 testes round-trip skippados quando ext-zip ausente — OpenSpout precisa; styling de header dropped após `setHeaderStyle()` quebrar testes), FIELDS-ADV-005 (`RepeaterField` PHP — final extends Field; setters schema/minItems/maxItems/reorderable/collapsible/cloneable/itemLabel/relationship; `maxItems < minItems` lança `InvalidArgumentException`; non-Field schema entries silently filtered; macro `repeater`. **Hydration/persistence HasMany deferred** para Resource lifecycle hooks; React side é FIELDS-JS-XXX). Testes: mcp 45, export 23 (17 + 6 skip), fields-advanced 55. **A6 e C6 entregues auto-completos**; B6 inconclusivo, fechado manualmente (dropped header styling + ext-zip skip).
- **Batch paralelo #5** (3 sub-agentes, merged 2026-04-29): WIDGETS-008 (`Dashboard::findWidget` com extract de `resolveEntry` helper privado partilhado com `resolve`; `WidgetDataController::show` com 404 dashboard/widget desconhecido + 403 canSee + filters passthrough; rota `GET /admin/dashboards/{id}/widgets/{wid}/data`; `EchoFiltersWidget` fixture), MCP-004 (`DescribeResourceTool` com schema canónico, validação `slug`, optional Closure resolver, defensiva por campo — `class/slug/model` strict, `label/pluralLabel` degradam para mensagem de erro, `navigation*` degradam para null; auto-registro em `packageBooted`), FIELDS-ADV-004 (`CodeField` extends Field — language/theme/lineNumbers/wordWrap/tabSize/minHeight/readonly setters; tabSize clamped ≥ 1; minHeight clamped ≥ 0 com null reset; macro `code` em FieldFactory). Testes: widgets 116, mcp 38, fields-advanced 39. **Apenas C5 e B5 entregues auto-completos**; A5 ficou inconclusivo (sem feature test do controller) e foi fechado manualmente.
- **Batch paralelo #4** (3 sub-agentes, merged 2026-04-29): WIDGETS-007 (`DashboardController` com método único `show(Request, DashboardRegistry, ?string)` default `'main'`, abort 404, render Inertia `'arqel::dashboard'` com `dashboard` + `filterValues`; rotas `GET /admin` + `GET /admin/dashboards/{id}` em `routes/admin.php` registadas via `hasRoute('admin')` com middleware `web` + `auth`), MCP-003 (`ListResourcesTool` com `schema()` + `__invoke`, defensiva via try/catch por resource, constructor aceita `?Closure $resolver` para testes — refator do agente após `extends final ResourceRegistry` ter falhado; auto-registro em `McpServiceProvider::packageBooted` via `registerTool`), EXPORT-002 (`CsvExporter` real com `spatie/simple-excel` movido de suggest para require, `formatCell` para date/boolean/relationship/scalar, helper estático `streamDownload` para HTTP). Testes: widgets 107, mcp 31, export 18. Apenas A4 (widgets) entregue auto-completo; B4/C4 ficaram inconclusivos e foram fechados manualmente (correção do mock ResourceRegistry, remoção de CsvExporterStub assertion, lint pass).
- **Batch paralelo #3** (3 sub-agentes, merged 2026-04-29): TENANT-009 (tenant switcher backend — `SupportsTenantSwitching` contract opcional, `AbstractTenantResolver` com defaults para `tenants` relation + `current_tenant_id` column, `TenantManager::availableFor/canSwitchTo/switchTo`, `TenantSwitched` event, `TenantSwitcherController` com switch/list, rotas `POST /admin/tenants/{id}/switch` + `GET /admin/tenants/available`), EXPORT-001 (scaffold `arqel/export` — `ExportFormat` enum CSV/XLSX/PDF + `Exporter` contract + 3 stubs + `ExportAction` extends Action com type='bulk'), AUDIT-001 (scaffold `arqel/audit` — `LogsActivity` trait wrapper sobre Spatie defaults + `ActivityLogController` paginado + hard dep `spatie/laravel-activitylog ^4.10`). Tenant 103 testes, export 12, audit 11. Apenas C3 (audit) entregue automaticamente; A3 e B3 ficaram inconclusivos e foram completados manualmente. Adicionados scopes `export` ao commitlint.
- **Batch paralelo #2** (3 sub-agentes em worktrees isolados, merged 2026-04-29): WIDGETS-006 (Dashboard multi-instance + DashboardRegistry singleton, refatoração com props readonly + factory + responsive columns + class-string resolution), MCP-002 (JSON-RPC 2.0 core + tool/resource/prompt dispatch + stdio loop, McpDispatchException para `-32601`/`-32602`), FIELDS-ADV-003 (MarkdownField config-only com preview modes + syncScroll). Testes Pest: widgets 102, mcp 24, fields-advanced 26 — todos verdes.
- Estrutura inicial do monorepo (INFRA-001)
- Orquestração pnpm workspace + Composer path repositories (INFRA-002)
- Toolchain de lint/format (Pint, PHPStan, Biome) + git hooks (Husky + lint-staged + commitlint) (INFRA-003)
- Pipeline CI/CD em GitHub Actions (ci, test-matrix, security, docs-deploy, release) + Dependabot grouping (INFRA-004)
- Renovate Bot config com grouping completo + Dependabot reduzido a github-actions (INFRA-005)
- Política de segurança expandida (SLAs, disclosure process, âmbito) (GOV-001)
- Guia de contribuição + PR template + Issue templates (bug/feature/question) (GOV-003)
- Esqueleto do pacote `arqel/core` com composer.json, PSR-4, README, SKILL, phpunit/pest configs (CORE-001)
- `Arqel\Core\ArqelServiceProvider` com auto-discovery, registos singleton (`ResourceRegistry`, `PanelRegistry`), facade `Arqel`, `config/arqel.php`, comando `arqel:install` e suite Pest com Orchestra Testbench (CORE-002)
- Comando `arqel:install` com Laravel Prompts: publica `config/arqel.php`, faz scaffold de `app/Arqel/Resources`, `app/Arqel/Widgets`, `resources/js/Pages/Arqel`, gera `App\Providers\ArqelServiceProvider`, `resources/views/arqel/layout.blade.php` e `AGENTS.md` (RF-DX-08). Stubs em `packages/core/stubs/`. Flag `--force` sobrescreve sem prompt (CORE-003)
- Contract `Arqel\Core\Contracts\HasResource` (7 métodos estáticos: model, slug, label, plural, navigation icon/group/sort) e `Arqel\Core\Resources\ResourceRegistry` com `register`, `registerMany`, `discover` (PSR-4 + Symfony Finder), `findByModel`, `findBySlug`, `has`, `clear` e `all`. Idempotente; `InvalidArgumentException` em classes que não implementem o contract (CORE-004)
- `Arqel\Core\Panel\Panel` fluent builder (path, brand, theme, primaryColor, darkMode, middleware, resources, widgets, navigationGroups, authGuard, tenant) com getters tipados; `Arqel\Core\Panel\PanelRegistry` create-or-get com `panel(id)`, `setCurrent`/`getCurrent`, `all`, `has`, `clear`; `PanelNotFoundException` em `setCurrent` desconhecido. Routing real adiado para CORE-006 (`ResourceController`) (CORE-005)
- Classe abstract `Arqel\Core\Resources\Resource` (base para Resources do utilizador): static props `$model/$label/$pluralLabel/$slug/$navigationIcon/$navigationGroup/$navigationSort/$recordTitleAttribute`, auto-derivation de slug (`UserResource`→`users`)/label/plural label, lifecycle hooks (`beforeCreate`/`afterCreate`/`beforeUpdate`/`afterUpdate`/`beforeSave`/`afterSave`/`beforeDelete`/`afterDelete`), `recordTitle`/`recordSubtitle`/`indexQuery`. `LogicException` quando `$model` não é declarado. Implementa `HasResource`, `HasFields`, `HasActions`. Contracts adicionados: `HasFields` (requer `fields(): array`), `HasActions` (marker), `HasPolicies` (`getPolicy()`) (CORE-008)
- Comando `arqel:resource {model} {--with-policy} {--force}`: resolve `App\Models\X` para nomes curtos, gera `app/Arqel/Resources/{Model}Resource.php` a partir de `stubs/resource.stub`, respeita `arqel.resources.path/namespace`, `make:policy` interno em `--with-policy`. `--from-model`/`--from-migration` adiados até `Field` existir (FIELDS-*) (CORE-009)
- Blade root view `arqel::app` (`resources/views/app.blade.php`): doctype, `<title inertia>`, CSRF token, FOUC guard de tema (system/light/dark via `localStorage`), `@routes` (Ziggy opcional), `@viteReactRefresh`, `@vite`, `@inertiaHead`, `@inertia`. Config `arqel.inertia.root_view` aponta para `arqel::app`. Publicável via `vendor:publish --tag=arqel-views` (CORE-012)
- Traduções em `en` e `pt_BR` (`resources/lang/{locale}/{messages,actions,table,form,validation}.php`) com namespace `arqel::*`. Cobre actions, confirmation, flash, errors, table sort/pagination/filtros/bulk, form submit/reset/required, validation failed (CORE-013)
- SKILL.md actualizado para reflectir o âmbito real entregue (sem prometer `ResourceController`, `FieldSchemaSerializer`, `Renderable` que ainda não existem)
- Esqueleto do pacote `arqel/fields` (PHP 8.3+, Laravel 12|13, depende de `arqel/core` via path repository): `FieldServiceProvider` registado via auto-discovery, PSR-4 `Arqel\Fields\` → `src/`, Pest + Orchestra Testbench, smoke tests verificando boot do provider e autoload do namespace. Adicionado a `require-dev` do monorepo root como path repo. Tipos concretos e `FieldFactory` chegam em FIELDS-002+ (FIELDS-001)
- `Arqel\Fields\Field` abstract base com construtor `final` (subclasses só declaram `$type`/`$component`), auto-derivation de label a partir do nome (`first_name`→"First Name"), fluent API (`label`/`placeholder`/`helperText`/`default`/`readonly`/`disabled`/`columnSpan`/`columnSpanFull`/`dehydrated`/`live`/`liveDebounced`/`afterStateUpdated`), getters tipados, `isDisabled`/`isDehydrated` aceitam `Closure(?Model)` para avaliação contextual, `getTypeSpecificProps()` por defeito retorna `[]`. Concerns (`HasValidation`/`HasVisibility`/`HasDependencies`/`HasAuthorization`) não estão aplicados ainda — chegam em FIELDS-015..018 (FIELDS-002)
- `Arqel\Fields\FieldFactory` com infrastructure transversal (sem factory methods concretos, que chegam com cada tipo): `register(type, fieldClass)` valida via `is_subclass_of`, `hasType`, `macro(name, Closure)`, `hasMacro`, `flush` (apenas para testes), `__callStatic` que prefere macros sobre registry e lança `BadMethodCallException` para chamadas desconhecidas. Resolve dilema de nomenclatura (não colide com `Field` abstract); alias público `Field::*` virá quando os tipos concretos chegarem (FIELDS-003)
- 6 tipos de input texto em `src/Types/`: `TextField` (base extensível com `maxLength`/`minLength`/`pattern`/`autocomplete`/`mask`), `TextareaField` (rows/cols), `EmailField` (default rule `email`), `UrlField` (default rule `url`), `PasswordField` (`revealable`), `SlugField` (`fromField`/`separator` por defeito `-`). Registados no `FieldServiceProvider::packageBooted` como `text`/`textarea`/`email`/`url`/`password`/`slug`. `getTypeSpecificProps()` filtra valores `null` para payload limpo. Default rules expostas via `getDefaultRules()` aguardando integração com `HasValidation` (FIELDS-015) (FIELDS-004)
- `NumberField` (extensível, `min`/`max`/`step`/`integer()`/`decimals(int)`) com `getDefaultRules()` que emite `numeric`/`integer` + `min:`/`max:` conforme configurado. `CurrencyField` extends Number com `prefix` (default `$`), `suffix` opcional, `thousandsSeparator` (default `,`), `decimalSeparator` (default `.`), `decimals` default 2. Registados como `number`/`currency`. PT-BR usa `prefix('R$')->thousandsSeparator('.')->decimalSeparator(',')` (FIELDS-005)
- `BooleanField` (extensível, `type='boolean'`, `component='Checkbox'`, default `false`, `inline()` para layout horizontal vs stacked) com `getDefaultRules() = ['boolean']`. `ToggleField` extends Boolean (`type='toggle'`, `component='Toggle'`) com `onColor`/`offColor`/`onIcon`/`offIcon` opcionais filtrados quando não definidos. Registados como `boolean`/`toggle` (FIELDS-006)
- `SelectField` (extensível) com 3 modos de options (estáticas via `options(array)`, lazy via `options(Closure)`, relationship via `optionsRelationship(relation, display, ?query)` — resolução adiada para controller em CORE-006). Setters `searchable`/`multiple`/`native`/`creatable`/`createOptionUsing`/`allowCustomValues`. `MultiSelectField` (final, multiple=true, native=false). `RadioField` (final, native=false). Registados como `select`/`multiSelect`/`radio` (FIELDS-007)
- `BelongsToField` (final) configurada via factory `make($name, $relatedResource)` (Field::__construct é final por design FIELDS-002). Valida `is_subclass_of(..., HasResource::class)` em runtime, deriva `relationshipName` removendo sufixo `_id`. Setters `searchable`/`preload`/`searchColumns`/`optionLabel(Closure)`/`relationship(name, ?query)`. `HasManyField` (final, readonly Phase 1) com `canAdd`/`canEdit` aceites como flags forward-compat (Phase 2). Search/preload/create routes são metadata; resolução real adiada para CORE-006. Registados como `belongsTo`/`hasMany` (FIELDS-008)
- `DateField` (extensível, default `format='Y-m-d'`/`displayFormat='d/m/Y'`) com `minDate`/`maxDate` aceitando `string|Closure` (closures resolvidas em serialize-time, retornos não-string descartados graciosamente), `format`/`displayFormat`/`closeOnDateSelection`/`timezone`. `getDefaultRules()=['date']`. `DateTimeField` (final extends Date) com `format='Y-m-d H:i:s'`/`displayFormat='d/m/Y H:i'` e `seconds(bool)` que flipa display para `H:i:s`. Registados como `date`/`dateTime` (FIELDS-009)
- `FileField` (extensível, config-only) com `disk`/`directory`/`visibility`/`maxSize(int kilobytes)`/`acceptedFileTypes(array mimes)`/`multiple`/`reorderable` (auto-flips multiple)/`using(strategy)`. Constantes `STRATEGY_DIRECT`/`STRATEGY_SPATIE_MEDIA_LIBRARY`/`STRATEGY_PRESIGNED` e `VISIBILITY_PRIVATE`/`VISIBILITY_PUBLIC`. `getDefaultRules()` emite `file|array` + `max:` + `mimetypes:` para single-file mode. `ImageField` (final extends File) com defaults de mime gate (`image/jpeg`/`png`/`webp`), `imageCropAspectRatio`/`imageResizeTargetWidth`. `handleUpload`/`handleDelete` e endpoint POST adiados para CORE-006. Registados como `file`/`image` (FIELDS-010)
- `ColorField` (final) com `presets(array)`/`format('hex'|'rgb'|'hsl')`/`alpha(bool)` e constantes tipadas `FORMAT_HEX/RGB/HSL`. `HiddenField` (final, type='hidden', component='HiddenInput') para passing IDs sem UI. `SlugField` extendido com `reservedSlugs(array)` (emite rule `not_in:`) e `unique(class-string, ?column)` (emite rule `unique:table,col` resolvendo `getTable()` quando disponível, com fallback heurístico). Registados como `color`/`hidden` (`slug` já em FIELDS-004) (FIELDS-011)
- `Arqel\Fields\ValidationBridge` traduz arrays de regras Laravel para uma string de schema Zod (ex: `z.string().email().min(1).max(255).nullable()`). 19 translators built-in: `string`/`numeric`/`integer`/`boolean`/`array`/`date`/`file`/`image`/`email`/`url`/`uuid`/`min`/`max`/`size`/`regex`/`in`/`not_in`/`unique`/`nullable`/`required`/`mimetypes`. `register(string, Closure)` para custom rules; `hasRule()`, `flush()` (testes). Unknown rules saltadas silenciosamente para não quebrar com regras server-only (`confirmed`). Helper `Translation` agrega tipo + chain calls + flags `nullable`/`required` (FIELDS-012)
- 21 snapshot tests do shape JSON canónico de cada Field type (`tests/Snapshots/{type}.json`) — funcionam como contrato visível para o lado React e detectam mudanças acidentais de payload. Self-bootstrap (cria snapshot na primeira run, valida byte-equality nas seguintes). Feature tests de search/upload/createOption adiados para CORE-006 (controller) (FIELDS-013 parcial)
- `packages/fields/SKILL.md` reescrito para reflectir o estado real (21 tipos entregues + ValidationBridge + snapshots) com tabela completa de tipos, exemplos copy-pasteáveis (Resource típico, Currency PT-BR, custom select), guia "Creating custom fields" passo-a-passo, secção Macros, ValidationBridge, conventions e anti-patterns (FIELDS-014)
- `Arqel\Fields\Concerns\HasValidation` trait aplicada à classe base `Field`: `required(bool|Closure)` (idempotente, `false` faz drop), `nullable`, `rules(array)`, `rule(string|object, ?$message)`, `unique(?$table, ?$column, mixed $ignorable)`, `maxLength`/`minLength`, `requiredIf`/`requiredWith`/`requiredWithout`, `validationAttribute`, `validationMessage`. `getValidationRules()` resolve Closures e dedupe contra `getDefaultRules()` (subclasses). Rule objects (e.g. `new Password(8)`) preservados verbatim. `SlugField::unique` renomeado para `uniqueIn` para não colidir com a nova API base. Snapshots regenerados para incluir `rules` array (FIELDS-015)
- `Arqel\Fields\Concerns\HasVisibility` trait aplicada à classe base `Field`: 4 contextos (`create`/`edit`/`detail`/`table`) controlados via `hidden(bool)`, `hiddenOnCreate`/`hiddenOnEdit`/`hiddenOnDetail`/`hiddenOnTable`, `visibleOn(string|array)` (whitelist), `hiddenOn(string|array)`, `visibleIf(Closure)` / `hiddenIf(Closure)` (mutuamente exclusivos — `LogicException` se combinados). `isVisibleIn(context, ?Model)` é o oracle único. `InvalidArgumentException` em context names desconhecidos (FIELDS-016)
- `Arqel\Fields\Concerns\HasDependencies` trait aplicada à classe base `Field`: `dependsOn(string|array)` com dedupe automático, `resolveOptionsUsing(Closure)`, `getDependencies`/`hasDependencies`/`getResolveOptionsCallback`. `handleDependencyUpdate(formState, changedField)` é o oracle server-side (controller CORE-006 vai chamá-lo): retorna `[]` quando não há resolver ou quando o changedField não está nas deps; resolver retorno não-array é coerced para `[]`. Quando não há deps declaradas, qualquer changedField dispara o resolver (manual refresh) (FIELDS-017)
- `Arqel\Fields\Concerns\HasAuthorization` trait aplicada à classe base `Field`: `canSee(Closure)` e `canEdit(Closure)` registam predicados; `canBeSeenBy(?Authenticatable, ?Model)` e `canBeEditedBy(?Authenticatable, ?Model)` são oracles default-true. `canBeEditedBy` cascata `canBeSeenBy=false` para `false`. `HasManyField::canAdd`/`canEdit` renomeados para `canAddRecords`/`canEditRecords` para não colidir (FIELDS-018)
- `Arqel\Fields\EagerLoadingResolver::resolve(array $fields): array<int, string>` extrai relation names de `BelongsToField` e `HasManyField` para `Builder::with(...)`. Dedupe automático, ignora non-Field entries graciosamente, honra `relationship()` overrides. Integração no `ResourceController::index()` virá com CORE-006 (RF-P-03, ADR-003 §Consequences) (FIELDS-019)
- `Arqel\Fields\ArqelFields::skill()` retorna inventário JSON-serialisable dos tipos registados + macros (consumido pelo MCP server na Fase 2). `FieldFactory::getRegisteredTypes()` e `FieldFactory::getRegisteredMacros()` expostos como API pública. Comando Artisan `arqel:field {name} {--force}` gera `app/Arqel/Fields/{Name}Field.php` (PHP) + `resources/js/Arqel/Fields/{Name}Input.tsx` (React stub) + imprime hint de registo no ServiceProvider. Stubs em `packages/fields/stubs/field.stub` + `field-component.stub` (FIELDS-022)
- Esqueleto do pacote `arqel/table` (PHP 8.3+, Laravel 12|13, depende de `arqel/core`): `TableServiceProvider` via auto-discovery, smoke tests + skeleton (TABLE-001)
- `Arqel\Table\Table` fluent builder com columns/filters/actions/bulkActions/toolbarActions, `defaultSort`, `perPage`, `searchable/selectable/striped/compact`, `emptyState`. `toArray()` serializa schema para Inertia payload (TABLE-002)
- `Arqel\Table\Column` abstract + 9 tipos concretos (`TextColumn`/`BadgeColumn`/`BooleanColumn`/`DateColumn`/`NumberColumn`/`IconColumn`/`ImageColumn`/`RelationshipColumn`/`ComputedColumn`) com fluent API e `Column::make($name)` factory (TABLE-003)
- `Arqel\Table\Filter` abstract + 6 tipos concretos (`SelectFilter`/`MultiSelectFilter`/`DateRangeFilter`/`TextFilter`/`TernaryFilter`/`ScopeFilter`) com `applyToQuery()` oracle e `apply(Closure)` override (TABLE-004)
- `Arqel\Table\TableQueryBuilder` orquestra search/filter/sort/eager-load/paginate da request → Eloquent query. Sort whitelisted contra colunas sortable; per_page validado contra getPerPageOptions; eager loading inferido de RelationshipColumn (TABLE-005)
- Esqueleto do pacote `arqel/form` (PHP 8.3+, Laravel 12|13, depende de `arqel/core`/`arqel/fields` via path repositories): `FormServiceProvider`, PSR-4 `Arqel\Form\` → `src/`, Pest + Orchestra Testbench, smoke tests verificando boot do provider e autoload do namespace (FORM-001)
- `Arqel\Form\Form` fluent builder (final): `make()`, `schema(array)`, `columns(int)` (clamp ≥ 1), `model(class-string<Model>)`, `inline(bool)`, `disabled(bool)`. `getFields()` flatten recursivo descendo em layout components; `toArray()` serializa schema com `kind: field|layout` para o payload Inertia (FORM-002)
- `Arqel\Form\Layout\Component` abstract base para layout components (Section/Fieldset/Grid/Columns/Group/Tabs/Tab): estado partilhado (`$schema`/`$columnSpan`/`$visibleIf`/`$canSee`), fluent `schema`/`columnSpan`/`visibleIf`/`canSee`, oracle `isVisibleFor(?Model)` que avalia `canSee` antes de `visibleIf`, template method `getTypeSpecificProps()` e `toArray()` final emitindo `{type, component, columnSpan, props}`. `Section` (heading/description/icon/collapsible/collapsed/columns/compact/aside; `collapsed()` implica `collapsible()`). `Fieldset` (legend + columns) (FORM-003, FORM-004 parcial)
- `Arqel\Form\Layout\Grid` (colunas fixas `int` ou breakpoint map `array<string,int>`, gap), `Arqel\Form\Layout\Columns` (atalho semântico para 2 colunas), `Arqel\Form\Layout\Group` (sem chrome visual; `orientation('horizontal'|'vertical')` com fallback para vertical em valores inválidos; `visibleIf`/`canSee` aplicam-se ao bloco) (FORM-004)
- `Arqel\Form\Layout\Tabs` (`tabs(array<Tab>)`, `defaultTab(id)` com fallback para o primeiro Tab, `vertical()`/`horizontal()` orientation) e `Arqel\Form\Layout\Tab` (`id`, `label`, `icon`, `badge(int|Closure)` resolvido em serialize-time com retornos não-int descartados) (FORM-005)
- SKILL.md do pacote `arqel/form` reescrito de raiz: status real (FORM-001..005 entregue, FORM-006/008 adiados por dependência em CORE-006), Key Contracts (Form fluent API + tabela dos 7 layout components), 3 exemplos copy-pasteáveis (Section+Grid, Tabs+visibility, Group invisible), Conventions e 5 Anti-patterns (FORM-010 parcial)
- Esqueleto do pacote `arqel/actions` (PHP 8.3+, Laravel 12|13, depende de `arqel/core`/`arqel/fields`/`arqel/form` via path repositories): `ActionsServiceProvider` registado via auto-discovery, PSR-4 `Arqel\Actions\` → `src/`, Pest + Orchestra Testbench, smoke tests verificando boot do provider e autoload do namespace. Adicionado a `require-dev` do monorepo root como path repo. `Action` base, tipos concretos (RowAction/BulkAction/ToolbarAction/HeaderAction), concerns e `ActionExecutor` chegam em ACTIONS-002+ (ACTIONS-001)
- `Arqel\Actions\Action` abstract base com `final __construct`, fluent API completo (`label`/`icon`/`color`/`variant`/`action(Closure)`/`url(Closure\|string, method)`/`visible`/`disabled`/`hidden`/`tooltip`/`successNotification`/`failureNotification`), constantes tipadas `COLOR_*`/`VARIANT_*`/`METHOD_*`, modo XOR action↔url (`action()` limpa url e vice-versa), oracles `isVisibleFor`/`isDisabledFor`/`resolveUrl(record)`, `execute(record, data)` invoca callback (retorna `null` sem callback), `toArray(?Authenticatable, mixed)` filtra chaves `null`. Auto-deriva label do nome via `Str::title('snake_case')` (ACTIONS-002)
- `Arqel\Actions\Types\RowAction`/`ToolbarAction`/`HeaderAction`/`BulkAction` (4 tipos finais). `BulkAction` com constante `DEFAULT_CHUNK_SIZE=100`, `chunkSize(int)` (clamp ≥ 1), `deselectRecordsAfterCompletion(bool)`, `execute()` override que itera `Collection` em chunks chamando o callback parente por chunk; inputs não-Collection delegam ao parent. Factory `Arqel\Actions\Actions` com helpers `view/edit/delete/restore/create/deleteBulk` (delete e deleteBulk pre-confirmam destructive) (ACTIONS-003)
- `Arqel\Actions\Concerns\Confirmable` trait aplicada à classe base `Action` com constantes tipadas `MODAL_COLOR_DESTRUCTIVE/WARNING/INFO`. Setters `modalHeading`/`modalDescription`/`modalConfirmationRequiresText` auto-activam `requiresConfirmation=true`. `modalColor` rejeita valores fora da lista (cai para `destructive`). `getConfirmationConfig()` retorna `null` quando não requer confirmação, ou array filtrado com `heading/description/icon/color/requiresText/submitLabel/cancelLabel`. `Arqel\Actions\Concerns\HasAuthorization` trait aplicada à base com `authorize(Closure)` + `canBeExecutedBy(?Authenticatable, $record)` (default true sem callback) (ACTIONS-004)
- `Arqel\Actions\Concerns\HasForm` trait aplicada à `Action`: `form(array)` aceita lista heterogénea e filtra para apenas instâncias de `Arqel\Fields\Field` (entries inválidos descartados graciosamente); constantes tipadas `MODAL_SIZE_SM/MD/LG/XL/FULL` com `modalWide(bool)` (alterna md↔lg) e `modalSize(string)` (fallback md em valores inválidos); `hasForm()` indica se há fields configurados; `getFormFields()` retorna a lista; `getFormValidationRules()` agrega `getValidationRules()` por nome de field; `getFormSchemaArray()` produz `[{name,type}, ...]` para o payload Inertia. `Action::toArray()` agora inclui `form`/`modalSize` quando há form (ACTIONS-005)
- SKILL.md do pacote `arqel/actions` reescrito de raiz com Status real (ACTIONS-001..005 entregue, ACTIONS-006/007 adiados por dependência em CORE-006), Key Contracts (Action fluent API, tabela dos 4 tipos, Confirmable, HasForm, Actions factory), 5 exemplos copy-pasteáveis (callback simples, destructive com confirmação, bulk com chunking, action com form modal, action como link), Conventions e 6 Anti-patterns (ACTIONS-008/009 parcial — testes Pest cobertos por unit tests; Feature tests cheios adiados até ActionController existir)
- Esqueleto do pacote `arqel/auth` (PHP 8.3+, Laravel 12|13, depende de `arqel/core` via path repository): `AuthServiceProvider` registado via auto-discovery, regista `AbilityRegistry` e `PolicyDiscovery` como singletons, PSR-4 `Arqel\Auth\` → `src/`, Pest + Orchestra Testbench. Adicionado a `require-dev` do monorepo root (AUTH-001)
- `Arqel\Auth\AbilityRegistry` final: `registerGlobal(string)`/`registerGlobals(array)` com dedupe automático; `registerComputed(string, Closure)`; `resolveForUser(?Authenticatable)` retorna `array<ability, bool>` resolvendo globals via `Gate::forUser($user)->allows($ability)` (false para guests) e computed via closure invocada com `$user`; cache per-request por `getAuthIdentifier()` (`'guest'` para null); `clear()` limpa registry e cache. Inteiro 7 testes Pest cobrindo dedupe, guest-as-false, cache hit, resolução combinada (AUTH-003)
- `Arqel\Auth\PolicyDiscovery` final: `autoRegisterPoliciesFor(array<class-string>)` retorna `['registered' => array<modelClass, policyClass>, 'missing' => array<resourceClass>]`. Heurística: troca `\Models\` por `\Policies\` no namespace do model e adiciona sufixo `Policy`. Honra `Resource::$policy` override (verificado com `property_exists` + `class_exists`). Resources que lançam exceção em `getModel()` ou que não existem são skippados graciosamente. Emite `LoggerInterface::warning` por Policy ausente. Regista via `Gate::policy(modelClass, policyClass)`. 4 testes Pest cobrindo guess, override, missing, classe inexistente (AUTH-002)
- `Arqel\Auth\ArqelGate` final: facade integrada ao `AbilityRegistry` com `register(string, Closure)`/`abilities(string ...)` (proxies do registry), `allows`/`denies(ability, args = [])` (via `Gate::forUser(Auth::user())`) e `snapshot()` (alias para `resolveForUser(Auth::user())`)
- Esqueleto do pacote `arqel/nav` (PHP 8.3+, Laravel 12|13, depende de `arqel/core` via path repository): `NavServiceProvider` registado via auto-discovery, regista `Navigation` e `BreadcrumbsBuilder` como singletons, PSR-4 `Arqel\Nav\` → `src/`, Pest + Orchestra Testbench. Adicionado a `require-dev` do monorepo root (NAV-001)
- `Arqel\Nav\NavigationItem` final com fluent API (`label`/`icon`/`url`/`route(name, params)`/`openInNewTab`/`visible(Closure)`/`badge(int\|string\|Closure)`/`badgeColor`/`sort`), modos XOR url↔route (cada um limpa o outro), factory `NavigationItem::resource(class-string)` que extrai label/icon/sort/URL automaticamente de `getPluralLabel`/`getNavigationIcon`/`getNavigationSort`/`getSlug` quando presentes, oracle `isVisibleFor(?Authenticatable)`, `resolveBadge()` invoca closure descartando retornos não-int|string, `toArray()` filtra chaves null. `Arqel\Nav\NavigationGroup` final colapsável (`collapsed()` implica `collapsible()`), `items(array)` rejeita não-NavigationItem, `addItem(NavigationItem)` para acumulação dinâmica, `toArray()` ordena items por sort e filtra por visibility (NAV-002)
- `Arqel\Nav\Navigation` final: builder principal com `item(NavigationItem)`/`group(label, Closure(NavigationGroup))`/`addGroup(NavigationGroup)`/`divider()`, `autoRegister(bool)` opt-out, `autoRegisterFromResources(array<class-string>)` agrupa Resources por `getNavigationGroup()` (cria/reusa NavigationGroup) ou cai para top-level quando null, idempotente (não duplica resources já registados manualmente), `build(?Authenticatable)` retorna payload ordenado por sort com visibility filtrada e tipo `kind` (`item`/`group`/`divider`) (NAV-003)
- `Arqel\Nav\BreadcrumbsBuilder` final: `withResources(array<class-string>)` + `buildFor(routeName, params)` resolve breadcrumbs a partir do nome de rota Inertia (`arqel.{panel}.{resource}.{action}`). Suporta actions `index`, `create`, `edit`, `show`. Quando `params['record']` ou `params['model']` é `Model`, interpola `Resource::recordTitle($record)`; caso contrário usa `getKey()` ou um id scalar. Fallback para `ucfirst($slug)` quando Resource não está registado. Constantes tipadas `ACTION_INDEX/CREATE/EDIT/SHOW` (NAV-004)
- SKILL.md do pacote `arqel/nav` reescrito com Status real (NAV-001..004 entregue), Key Contracts (NavigationItem com 8 setters, NavigationGroup, Navigation builder, BreadcrumbsBuilder), 5 exemplos copy-pasteáveis, Conventions e 5 Anti-patterns (NAV-005 parcial — testes Pest cobertos por unit/feature; integração com ArqelServiceProvider para auto-registo automático adiada)
- `Arqel\Core\Http\Controllers\ResourceController` final: 7 endpoints polimórficos (`index/create/store/show/edit/update/destroy`) que resolvem Resource pelo `{resource}` slug via `ResourceRegistry::findBySlug` (404 se ausente), autorizam via `Gate::denies(viewAny|create|view|update|delete)` (silently allow quando não há Policy registada — Resource Policies são user-owned), materializam payload via `InertiaDataBuilder` e renderizam `Inertia::render('arqel::{action}', ...)`. Writes invocam `Resource::runCreate/runUpdate/runDelete` (ver §Resource lifecycle). `validated()` faz validation light dropping `_token/_method/resource/id` da request body — full FormRequest generation chega em FORM-007 (CORE-006)
- `Arqel\Core\Http\Middleware\HandleArqelInertiaRequests` final estende `Inertia\Middleware`. `version()` concatena Arqel version ao asset version do parent. `share()` adiciona shared props lazy: `auth.user` (`only(['id','name','email'])` quando user é Authenticatable, `null` para guests), `auth.can` (delegated to `Arqel\Auth\AbilityRegistry::resolveForUser` quando o pacote auth está bound, `[]` caso contrário), `panel` (id/path/brand do `PanelRegistry::getCurrent`), `tenant` (sempre null em Fase 1 — scaffold para Fase 2), `flash` (success/error/info/warning closures), `translations` (loader de `arqel::*`), `arqel.version` (CORE-007)
- `Arqel\Core\Support\InertiaDataBuilder` final: assembler dos payloads index/create/edit/show. `buildIndexData` faz paginate sanitizado (per_page/page coerced via is_numeric); `buildCreateData/EditData/ShowData` retornam `{resource, record, recordTitle, recordSubtitle, fields}`. `serializeFields` prefere `Field::toArray()` quando disponível, fallback para `{name, type}` via `getName/getType`, descarta entries não-Field (CORE-006 parcial)
- Routes file `packages/core/routes/arqel.php` com 7 rotas nomeadas `arqel.resources.{action}` (`{resource}` slug + `{id}` regex `[0-9a-zA-Z\-_]+` para PUT/PATCH/DELETE). `ArqelServiceProvider::registerResourceRoutes()` carrega o ficheiro com prefix do panel atual (ou `config('arqel.path')` fallback) e middleware stack do panel + `HandleArqelInertiaRequests` (deduplicado) (CORE-006)
- `Arqel\Core\Resources\Resource::runCreate(array)/runUpdate(Model, array)/runDelete(Model)` orchestrators públicos: `runCreate` corre `beforeSave → beforeCreate → fill+save → afterCreate → afterSave`; `runUpdate` corre `beforeSave → beforeUpdate → fill+save → afterUpdate → afterSave`; `runDelete` corre `beforeDelete → delete()`, com `afterDelete` apenas em sucesso (delete retornou truthy). Subclasses continuam a sobrescrever os hooks `protected`, não os orchestrators. `runCreate` instancia o model via `app($modelClass)` para permitir test doubles via container binding
- `Resource::table(): mixed` hook adicionado (default `null`). `InertiaDataBuilder::buildIndexData` agora detecta o retorno via duck-typing (`getColumns/getFilters` presentes) e roteia para `buildTableIndexData` que delega paginate ao `Arqel\Table\TableQueryBuilder` (resolvido via Reflection — sem hard dep) e serializa columns/filters/actions (row/bulk/toolbar). `arqel/core` continua independente de `arqel/table`. Records serializados ganham `arqel.title`/`arqel.subtitle` via `Resource::recordTitle`/`recordSubtitle` (TABLE-006)
- `Arqel\Actions\Http\Controllers\ActionController` final: 4 endpoints (`invokeRow/invokeHeader/invokeToolbar/invokeBulk`) routed sob `arqel.actions.{name}`. Resolve Resource via `ResourceRegistry::findBySlug`, action por nome em `actions/headerActions/toolbarActions/bulkActions` (todos opt-in via `method_exists` na Resource), autoriza via `Action::canBeExecutedBy`, valida payload do form modal quando `Action::hasForm()` é true, executa callback e flasha success/failure notification. Bulk fetcha records via `whereIn(getKeyName, ids)` antes de chamar `Action::execute(Collection)` que faz chunk automático. `ActionsServiceProvider` mounta as rotas sob o panel path + middleware (espelhando `arqel/core`) (ACTIONS-006)
- Routes file `packages/actions/routes/arqel-actions.php` com 4 rotas POST: bulk-actions, actions (toolbar), {id}/actions/{action} (row), {id}/header-actions/{action} (header). Action endpoints integrados na shared route prefix do panel — convivem com as rotas de `arqel/core` (ACTIONS-006)
- `Arqel\Form\FieldRulesExtractor` final: agrega regras/messages/attributes através de uma lista flat de `Field`s. `extract(array): array<name, rules>` deduzido de `Field::getValidationRules()`; `extractMessages(array): array<{name}.{rule}, message>` (namespacing exato da `FormRequest::messages()` Laravel); `extractAttributes(array): array<name, attribute>` para overrides de `validationAttribute`. Entries não-Field são silenciosamente descartados (FORM-007 parte 1)
- `Arqel\Form\FormRequestGenerator` final: gera `Store{Model}Request` (ability `create`) e `Update{Model}Request` (ability `update`) extending `FormRequest` a partir de uma class-string de Resource. `generate(class, action, namespace)` retorna o source PHP; `write(class, targetPath, force=false)` escreve ambas as variantes em disco, retornando a lista de ficheiros escritos (skip silente quando o ficheiro já existe e `force=false`). Stub em `stubs/form-request.stub` substitui `{{ namespace }}/{{ class }}/{{ resourceClass }}/{{ resourceClassShort }}/{{ ability }}`. As classes geradas delegam `rules()/messages()/attributes()` ao `FieldRulesExtractor` resolvendo o Resource via container (FORM-007 parte 2)
- `Arqel\Core\Http\Controllers\ResourceController::validated(Request, Resource)` agora extrai validation rules dos Fields da Resource via `Arqel\Form\FieldRulesExtractor` (carregado via Reflection — `arqel/core` continua independente de `arqel/form`). Chama `$request->validate($rules)` quando há regras; cai para o stripping permissivo (drop `_token`/`_method`/`resource`/`id`) quando o pacote form não está instalado. FormRequests hand-rolled continuam a ter precedência via type-hint resolution (FORM-007 integração)
- `Arqel\Auth\Concerns\AuthorizesRequests` trait: 3 oracles para Arqel controllers. `authorizeResource(class-string, action, ?record)` chama `Gate::denies(action, record ?? modelClass)` e aborta 403 (silently allow quando não há gate nem policy registada — Resource Policies são user-owned). `authorizeAction(object, ?record)` delega ao `Action::canBeExecutedBy` Closure. `authorizeField(object, 'view'\|'edit', ?record)` honra `canBeSeenBy`/`canBeEditedBy` do trait `HasAuthorization` em Fields (AUTH-004)
- `Arqel\Auth\Http\Middleware\EnsureUserCanAccessPanel` final: gate de panel-level com ability configurável via parâmetro de middleware (default `viewAdminPanel` exposto via constante `DEFAULT_ABILITY`). Aborta 401 para guests, 403 quando `Gate::denies`, allow-through quando a ability não está registada com Laravel Gate (uma fresh install boota sem panel-level gating). Aceita ability custom: `->middleware(EnsureUserCanAccessPanel::class.':manageSettings')` (AUTH-004)
- `arqel_can(string $ability, mixed $arguments = null): bool` global helper em `packages/auth/src/helpers.php` (autoloaded via composer `files`). Lookup order: `AbilityRegistry` snapshot (quando bound) primeiro, fallback para `Gate::forUser($user)->allows`. Retorna `false` para guests (AUTH-004)
- `ArqelGate` agora consome o trait `AuthorizesRequests` para garantir analise estática do trait. Sem mudança funcional
- `Arqel\Fields\Http\Controllers\FieldSearchController` final: GET `{panel}/{resource}/fields/{field}/search?q=...` retorna JSON `[{value, label}, ...]` (max 20). Resolve Resource via `ResourceRegistry::findBySlug` (404 se ausente), valida que o field é `BelongsToField` (400) e que tem search habilitada (403). Aplica `searchColumns()` configuradas (LIKE em cada uma) ou guess-list (`name`/`title`/`label`) intersectada com `fillable`. Honra `relationship(name, Closure)` para custom query e `optionLabel(Closure)` para labels custom. Fallback para `Resource::recordTitle` quando há (FIELDS-020)
- `Arqel\Fields\Http\Controllers\FieldUploadController` final: POST `{panel}/{resource}/fields/{field}/upload` valida o file contra `maxSize`/`acceptedFileTypes` do FileField, persiste via `UploadedFile::store($directory, $disk)`, devolve `{path, url, size, originalName}`. URL é resolvida via `Storage::disk($disk)->url($path)` quando o filesystem suporta (S3/public), null caso contrário. DELETE remove via `Storage::disk()->delete($path)`. 422 quando file/path ausentes, 400 quando o field não é `FileField`, 404 quando Resource ou field não existem (FIELDS-021)
- Routes file `packages/fields/routes/arqel-fields.php` com 3 rotas: GET search (rate-limited 30 req/min via `throttle:30,1`), POST upload, DELETE upload. `FieldServiceProvider::registerFieldRoutes()` mounta sob o panel path + middleware (espelhando `arqel/core` e `arqel/actions`) (FIELDS-020/021)
- Snapshots dos 21 tipos de Field regenerados após mudança em PHP 8.4 que alterou indentação default de `JSON_PRETTY_PRINT` (2→4 spaces). Sem mudança funcional nos campos
- `Arqel\Core\Support\FieldSchemaSerializer` final: serializer central de Fields para o payload Inertia (shape canônico em `06-api-react.md` §4). Duck-typed contra `Arqel\Fields\Field` (sem hard dep no `arqel/core`) — invoca `` quando presentes. Filtra fields por `canBeSeenBy(user, record)`, combina `isReadonly` com `canBeEditedBy` num único flag readonly, emite `validation.{rules,messages,attribute}`, `visibility.{create,edit,detail,table,canSee}`, `dependsOn` (lista de strings) e `props`. `stringifyRules` descarta Closures e converte rule objects para class-string (CORE-010)
- `Arqel\Core\Support\InertiaDataBuilder` agora delega serialização de Fields ao `FieldSchemaSerializer` (DI via construtor com default `new`). `buildCreateData/EditData/ShowData/buildPlainIndexData` passam `$record` + `Auth::user()` para o serializer, permitindo filtro per-record/per-user dos fields (CORE-010 integração)
- Inertia useForm flow validado: `ResourceController::store/update` invocam `validated(Request, Resource)` que lança `ValidationException` quando há regras (extraídas via `Arqel\Form\FieldRulesExtractor`), e Laravel/Inertia automaticamente converte em `back()->withErrors()->withInput()` para o client. Permissive fallback (drop `_token/_method/resource/id`) cobre o caso sem rules. Test Feature confirma o stripping (FORM-008)
- Routes file `packages/core/routes/arqel.php` agora marca `store`, `update` (PUT/PATCH) com `Route::middleware('precognitive')` — Laravel Precognition responde 204 No Content para dry-run requests com header `Precognition: true`. Stub Fase 1; field-level real-time validation expande em Fase 2 (FORM-008)
- SKILL.md do pacote `arqel/form` atualizado para refletir FORM-001..005/007/008/010 entregues
- Pacote npm `@arqel/types` (TypeScript types compartilhados, zero runtime). 7 entry points (`./`, `./fields`, `./resources`, `./tables`, `./forms`, `./actions`, `./inertia`) via `package.json` exports, `sideEffects: false` para tree-shaking. Build via `tsup` com `dts`/ESM/sourcemaps. `tsconfig.json` extende `tsconfig.base.json` (strict + `noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`) (TYPES-001)
- Types canônicos materializados em TypeScript: 21 `FieldType` em discriminated union sobre `type` (Text/Textarea/Email/Url/Password/Slug/Number/Currency/Boolean/Toggle/Select/MultiSelect/Radio/BelongsTo/HasMany/Date/DateTime/File/Image/Color/Hidden) com `props` per-type; 9 `ColumnType` + 6 `FilterType` discriminated; 7 layout entries em `FormSchema` (Section/Fieldset/Grid/Columns/Group/Tabs/Tab) com `kind: 'field'\|'layout'` discriminator; `ResourceMeta`/`ResourceIndexProps<T>`/`ResourceCreateProps`/`ResourceEditProps<T>`/`ResourceDetailProps<T>` (genéricos sobre `RecordType`); `ActionSchema` discriminado por type + `ConfirmationConfig` + `ActionFormField` + `ResourceActions`; `SharedProps` (Inertia) com `auth`/`panel`/`tenant`/`flash`/`translations`/`arqel.version` (TYPES-002)
- Type guards públicos: `isFieldType(field, type)` (narrow `FieldSchema`), `isFieldEntry(entry)`/`isLayoutEntry(entry)` (narrow `SchemaEntry`), `resolveFieldEntry(entry, fields)` (lookup de FieldEntry por nome). 20 type-level assertions via `expect-type` + Vitest passando (TYPES-002, TYPES-004 parcial)
- SKILL.md do `@arqel/types` com Status, Key Contracts (4 exemplos copy-pasteáveis), Conventions e 5 Anti-patterns
- Pacote npm `@arqel/react` (React + Inertia bindings, providers, utilities). 5 entry points (`./`, `./inertia`, `./providers`, `./context`, `./utils`) com tree-shake friendly subpath exports. `peerDependencies` para React 19 + `@inertiajs/react` 2 (REACT-001)
- `createArqelApp(options)` em `@arqel/react/inertia`: bootstrap Inertia com defaults Arqel — title callback (`${page} — ${appName}`), page resolver com fallback paths Vite-style, `<ArqelProvider>` wrap, progress bar configurável, SSR hydrate vs createRoot. `resolveArqelPage(registries, name)` aceita múltiplos `import.meta.glob` registries (REACT-002)
- `<ArqelProvider>` em `@arqel/react/providers`: wraps `PanelContext` + `TenantContext` + `<ThemeProvider>`. `<ThemeProvider>` light/dark/system com `localStorage` persist (`arqel-theme`), `prefers-color-scheme` listener auto-resolve, classe aplicada em `document.documentElement` para FOUC mitigation. `useTheme()` retorna `{ theme, resolved, setTheme, toggle }`. SSR-safe: localStorage só lido após mount (REACT-003)
- Contexts em `@arqel/react/context`: `PanelContext` + `usePanel()`/`useRequiredPanel()`, `ResourceContext` + `useResourceContext()`/`useRequiredResource()`, `TenantContext` (Phase 1 stub) + `useTenant()`. Variants `useRequired*` lançam erro claro quando o context não está bound (REACT-003)
- Utilities em `@arqel/react/utils`: `route(name, params, absolute)` typed wrapper sobre Ziggy (throws clear error quando ausente); `translate(dict, key, options)` + `useTranslator(dict)` com placeholders `:name`, retorna `key` como fallback (nunca undefined); `buildInitialFormState(fields, record)` semeia form state com type-appropriate defaults (boolean → false, multiSelect/hasMany → [], number/currency → null, others → ''); `indexFieldsByName(fields)`, `fieldsVisibleIn(fields, context)` (REACT-004)
- 20 testes Vitest passando (jsdom + `@testing-library/react`). Build via `tsup` com dts, ESM, sourcemaps, externalised React/Inertia. Biome + tsc strict limpos
- SKILL.md do `@arqel/react` com Key Contracts (5 exemplos copy-pasteáveis), Conventions e 5 Anti-patterns
- Pacote npm `@arqel/hooks` com 10 hooks reusáveis e 11 entry points subpath tree-shakeable: `useResource<T>()` (page props + ResourceContext merge), `useArqelForm({ fields, record })` (Inertia useForm com defaults via `buildInitialFormState`), `useCanAccess(ability, record?)` (record-level abilities têm precedência sobre globais — UX-only per ADR-017), `useFlash({ onMessage })` (callback once-per-new-message), `useTable()` (sort/filters/selection local; URL sync deferido para HOOKS-004 follow-up), `useAction(action)` (router.visit wrapper com processing flag), `useFieldDependencies()` (debounced 300ms partial reload de `fields.<name>.options`), `useNavigation()` (`panel.navigation` shared props), `useBreakpoint()` (Tailwind v4 breakpoint via `matchMedia`, SSR-safe), `useArqelOptimistic()` (React 19 `useOptimistic` wrapper). Inertia `useForm<T>` re-narrowed via `FormValues` bridge para evitar TS2589 mantendo o tipo de retorno público. 4 testes Vitest passando, peerDependencies para React 19 + Inertia 2 (HOOKS-001)
- Pacote npm `@arqel/ui` (componentes estruturais para admin panels) com 9 entry points subpath tree-shakeable (`shell`, `resource`, `table`, `form`, `action`, `auth`, `flash`, `utility`, `utils`). `globals.css` com Tailwind v4 `@import` + design tokens em `oklch` (`--color-arqel-bg/-fg/-primary/-secondary/-destructive/-success/-warning/-muted/-border/-ring`, `--radius-arqel/-sm/-lg`) + `.dark` override. `cn()` (clsx + tailwind-merge). `<Button>` cva (default/outline/ghost/destructive × sm/md/lg/icon). `<CanAccess>` wrapper sobre `useCanAccess` (UI-001)
- Shell (UI-002): `<AppShell variant>` 4 variants (`sidebar-left | sidebar-right | topbar-only | full-width`); `<Sidebar>` rail desktop fixo (240px via `--sidebar-width`) + Base UI Dialog overlay mobile quando `open`/`onOpenChange` fornecidos, items via `useNavigation()` ou prop, suporta groups, badges, `aria-current="page"`, focus ring; `<Topbar>` brand + theme toggle (usa `useTheme()`) + mobile menu trigger + slots search/userMenu/tenantSwitcher; `<MainContent>` padding responsivo + `maxWidth` configurável (`md..7xl | none`) + slots breadcrumbs/header; `<Footer>` minimal (UI-002)
- Table (UI-003): `<DataTable>` TanStack Table v8 com 9 cell renderers polimórficos (text/badge/boolean/date/number/icon/image/relationship/computed), seleção controlada `Shift+click` range select, sticky header, sort visual com `aria-sort`, loading row + customisable empty state, hidden + hiddenOnMobile honoured; `<TableFilters>` 4 tipos (select/multiSelect/text/ternary) + chip "Clear filters (n)" só quando ativos; `<TablePagination>` prev/next + range "11–20 of 47" + per-page picker; `<TableToolbar>` search + filters + bulk-action bar condicional ao `selectedCount`; `<ResourceIndex>` page-level wrapper costurando `ResourceIndexProps` server payload com toda a stack — presentational, callbacks lifted (UI-003)
- Form (UI-004): `<FormRenderer>` recursivo sobre `FormSchema` (Section/Fieldset/Grid/Columns/Group/Tabs/Tab + Field); `<FieldRenderer>` com label + asterisco required + helper/erro chained via `aria-describedby` + `role="alert"`, resolve via `FieldRegistry` global (`registerField('TextInput', RichInput)`) com fallback para native HTML inputs (17 dos 21 field types: text/email/url/password/slug/textarea/number/currency/boolean/toggle/select/multiSelect/radio/belongsTo/hasMany/date/dateTime/file/image); `<FormSection>` heading + description + collapsible body + opção `aside`; `<FormFieldset>` semantic com legend; `<FormGrid>` CSS-grid responsivo; `<FormTabs>` keyboard-accessible (Arrow/Home/End, roving tabindex, `role="tablist|tab|tabpanel"`); `<FormActions>` submit/cancel/processing (UI-004)
- Action (UI-005): `<ActionButton>` matriz de comportamento (requiresConfirmation only → ConfirmDialog, form only → ActionFormModal, both → ConfirmDialog → form, neither → fires onInvoke); `<ActionMenu>` inline buttons até `inlineThreshold` (3 default) ou Base UI dropdown menu acima; `<ConfirmDialog>` Base UI Dialog com title/description, "type to confirm" via `requiresText` (submit gated + Enter submit quando válido), cor drives variant; `<ActionFormModal>` Base UI Dialog hospedando `<FormRenderer>` inline de `action.form: ActionFormField[]` com `modalSize` configurável (sm..xl + full) (UI-005)
- Flash + utility (UI-006): `<FlashContainer>` consome `useFlash({ onMessage })` push-on-message + dismiss-on-timer-or-click, 4 posições (top-right default), pointer-events-none wrapper para não bloquear página; `<FlashToast>` self-rendered (sem `sonner` dep) com role=alert/aria-live=assertive para erros, role=status/aria-live=polite outros, `durationMs=0` opta-out auto-dismiss; `<Breadcrumbs>` items explicit ou auto via `panel.breadcrumbs` shared props (`usePage()` lazy só quando `items` omitido), `aria-current="page"` no último; `<PageHeader>` title + description + actions slot; `<EmptyState>` para coleções vazias; `<ErrorState>` 404/403/500 com role="alert"; `<LoadingSkeleton>` line/block/circle variants com count + Tailwind animate-pulse (UI-006)
- 70 testes Vitest passando em `@arqel/ui` (jsdom + `@testing-library/react` + `@testing-library/user-event`). Coverage report ≥ 67% global (barrels e Sidebar mobile-Dialog Portal são os principais miss). Build via `tsup` com 9 ESM entries + dts. Biome + tsc strict + `exactOptionalPropertyTypes` limpos. SKILL.md + README.md em PT-BR completos (UI-007)
- Pacote npm `@arqel/fields` (rich React inputs, registrados via `FieldRegistry` de `@arqel/ui`) com 12 entry points subpath (`./`, `./register`, `./text`, `./number`, `./boolean`, `./select`, `./relationship`, `./date`, `./file`, `./slug`, `./color`, `./hidden`) e 21 components 1:1 com PHP `FIELDS-001..022`: TextInput/TextareaInput/EmailInput/UrlInput/PasswordInput (toggle reveal `aria-pressed`), NumberInput (stepper buttons) + CurrencyInput (Intl-format on blur, raw on focus), Checkbox + Toggle (role=switch + iOS thumb), SelectInput (native) + MultiSelectInput (chips removíveis) + RadioGroup (role=radiogroup), BelongsToInput (async fetch + 300ms debounce ao `searchRoute`, role=combobox/listbox) + HasManyReadonly, DateInput + DateTimeInput (native HTML), FileInput (drag-drop em `<section>`) + ImageInput (URL.createObjectURL preview, sem crop em Phase 1), SlugInput + helper `slugify` (NFD + `[a-z0-9-]+`), ColorInput (native picker + presets + hex text), HiddenInput. `register.ts` é o único side-effect (`sideEffects: ['./dist/register.js']` em package.json) que popula `FieldRegistry` no boot via `import '@arqel/fields/register'`. `getRegisteredFields()` re-exportado de `@arqel/ui/form`. peerDeps `@arqel/ui` + React 19 + Inertia 2. 23 testes Vitest passando (registry roundtrip + behaviour/a11y per component). Build via `tsup` com 12 ESM entries + dts. SKILL.md PT-BR com guia "Creating a custom field" mostrando o triple PHP `Field::component()` + React component + `registerField` (FIELDS-JS-001..006)
- Bug fixes do dogfooding `arqel-test` (9 bugs reportados, todos fechados): **Bug 1** — `ArqelServiceProvider::packageBooted` agora agenda `app->booted` que copia `Panel::getResources()` para o `ResourceRegistry` global e elege primeiro panel como current (sem isso `/admin/{slug}` ficava 404 sempre). **Bug 2** — `HandleArqelInertiaRequests` override `$rootView = 'arqel::app'` + leitura de `config('arqel.inertia.root_view')` no construtor (Inertia padrão `app` não existia em fresh installs). **Bug 4** — `buildPlainIndexData` agora deriva `columns` de `fields()` honrando `visibility.table` + emite `filters: []`, `search`, `sort`, `actions: { row, bulk, toolbar }` (sem isso React `<ResourceIndex>` quebrava em `filters.length`). **Bug 5** — `app.blade.php` lê `config('arqel.inertia.vite_entries')` em vez de hardcoded paths. **Bug 6** — `@arqel/ui` ganha `tailwindcss: ^4.0.0` em peerDependencies. **Bug 8** — `MakeResourceCommand` reescreve a Policy gerada por `make:policy` com defaults Arqel-friendly (return true + TODOs) — o stub Laravel default retornava `false` em todos os métodos e quebrava o "Hello World" UX com 403. 4 testes Pest novos (`PanelToRegistrySyncTest`, 113/113 core total) cobrindo o sync, election, override explicit, skip de invalid resource entries. **Bug 3** — Novo subpath `@arqel/ui/pages` exportando `arqelPages: Record<string, LazyPage>` com `ArqelIndexPage`/`CreatePage`/`EditPage`/`ShowPage` (ResourceController emite `component: 'arqel::index'` mas o registry não existia). Apps fazem `pages: { ...arqelPages, ...userPages }` em `app.tsx` para herdar os defaults com possibilidade de override. **Bug 7** — `setup-test-app.sh` auto-inject `server.watch.ignored` no `vite.config` para `vendor/arqel/*/vendor/**` + `node_modules/**` (Vite ENOSPC com path-repo's vendor/* aninhados, ~65k files). **Bug 9** marcado wontfix (Panel::path slash inconsistente — `Route::prefix` aceita ambos formatos)
- Batch paralelo #1 — primeira execução com 3 sub-agentes em git worktrees isolados completou 6 tickets simultâneos (~15 min vs ~45-60 min serial): **WIDGETS-003 ChartWidget** (final, fluent: chartType com `CHART_*` constants + line fallback, height clamp ≥50, showLegend/showGrid, chartData/chartOptions array|Closure com Closure non-array fallback), **WIDGETS-004 TableWidget** (final, fluent: query Closure resolver, limit clamp ≥1, columns duck-typed, seeAllUrl, throwables capturados em loadError + records:[]; sem dep em arqel/table), **WIDGETS-005 CustomWidget** (final, escape hatch: make(name, component) factory, component validation não-vazio, withData(array|Closure) — setter renomeado de `data()` para `withData()` para preservar LSP do `Widget::data(): array`). **MCP-001** — esqueleto `arqel/mcp` (PHP 8.3+, Laravel 12|13, dep arqel/core via path repo): McpServer final stub com `registerTool/Resource/Prompt` no-op + getters retornando []; McpServiceProvider auto-discovered + singleton binding; phpunit + 4 smoke tests. Implementação real do JSON-RPC fica para MCP-002. **FIELDS-ADV-001/002** — esqueleto `arqel/fields-advanced` (PHP 8.3+, Laravel 12|13, deps arqel/core + arqel/fields): FieldsAdvancedServiceProvider registra macro `richText` em FieldFactory; **RichTextField** final extends Field com `type='richText'`/`component='RichTextInput'`, setters toolbar/imageUploadDisk/imageUploadDirectory/maxLength (clamp ≥1)/fileAttachments/customMarks/mentionable (filtra entries sem id+name); getTypeSpecificProps emite imageUploadRoute como string literal `/arqel/fields/upload?disk=<disk>` ou null; sem hard dep em HTML Purifier (sanitização é responsabilidade do consumer, documentada na class docblock). Merge: SKILL.md do widgets resolveu conflito manualmente (cluster A foi spawned antes de WIDGETS-002 mergear); 21 snapshot tests pre-existentes em `packages/fields/tests/Snapshots/*.json` regenerados (PHP 8.4 mudou JSON_PRETTY_PRINT indent de 2→4 spaces). composer.json raiz registra `arqel/mcp` + `arqel/fields-advanced`; commitlint scope `fields-advanced` adicionado. Suite global: 699 testes passando, phpstan clean, pint clean (WIDGETS-003/004/005 + MCP-001 + FIELDS-ADV-001/002, batch paralelo #1)
- Esqueleto do pacote `arqel/widgets` (Phase 2): PHP 8.3+, Laravel 12|13, dep em `arqel/core` via path repo. `Arqel\Widgets\Widget` (abstract) base com fluent API completo: construtor `(string $name)`, `heading`/`description`/`sort`/`columnSpan(int|string)` (clamp ≥1 quando int)/`poll(int)` (≤0 disable)/`deferred(bool)`/`canSee(Closure)`/`filters(array)`. Subclasses declaram `protected string $type` + `protected string $component` e implementam `data(): array`. `id()` default `<type>:<name>`. `toArray(?Authenticatable)` emite payload canônico Inertia (deferred → `data: null`, inline → calls `data()`). `Arqel\Widgets\Dashboard` (final) builder com `widgets(array)` (filtra non-Widget silently)/`addWidget`/`columns(int)` (clamp 1..12)/`heading`/`description`/`canSee(Closure)` + `toArray(?Authenticatable)` que filtra por `canBeSeenBy` + sorts por `getSort()` (null sorts last via PHP_INT_MAX). `Arqel\Widgets\WidgetRegistry` (final, singleton) com `register/has/get/all/clear`, validação `is_subclass_of(Widget)` lançando `InvalidArgumentException`. `WidgetsServiceProvider` auto-discovered + `phpunit.xml` padrão. 29 testes Pest passando, 65 assertions; SKILL.md PT-BR + README. Pacote registrado em `composer.json` raiz e symlinkado (WIDGETS-001)
- `Arqel\Tenant\Integrations\StanclAdapter` (final, `implements TenantResolver`) — adapter pass-through para [stancl/tenancy](https://tenancyforlaravel.com). Construtor `(string $modelClass)`. `resolve(Request)` busca `Stancl\Tenancy\Tenancy::tenant` via `Container::getInstance()->make(self::TENANCY_BINDING)` com guard `class_exists`; lança `LogicException` actionable (mensagem com `composer require stancl/tenancy`) quando lib não instalada ou TenancyServiceProvider não registado. `identifierFor()` honra `getTenantKey()` (convenção Stancl) com fallback `(string) getKey()`. Sem hard dep — class-name resolvida via constante string. `Arqel\Tenant\Integrations\SpatieAdapter` (final, `implements TenantResolver`) — adapter pass-through para [spatie/laravel-multitenancy](https://spatie.be/docs/laravel-multitenancy). Construtor `(string $modelClass)`. `resolve(Request)` chama `current()` static (gate `class_exists` + `method_exists`); `modelClass=''` cai no canonical `Spatie\\Multitenancy\\Models\\Tenant`. `identifierFor()` retorna `(string) getKey()`. Sem hard dep. 13 testes Pest novos (95 total, 142 assertions) via `class_alias()` simulando pacotes não instalados (TENANT-007/008)
- `Arqel\Tenant\Rules\ScopedUnique` (final, `implements ValidationRule`) — versão tenant-aware da rule `unique` do Laravel para single-DB tenancy. Construtor `(string $table, string $column, mixed $ignore=null, string $ignoreColumn='id', ?string $tenantForeignKey=null, ?string $connection=null)`. Resolve via `Container::getInstance()->make(ConnectionResolverInterface::class)` com fallback `'db'`; adiciona `where(<tenant_fk>, <id>)` quando `TenantManager::current()` retorna não-null; fallback global quando sem tenant; ignore via `where(<col>, '!=', $ignore)`; mensagem via `trans('validation.unique')` com fallback hardcoded. 7 testes Pest novos (83 total, 126 assertions) via mocks `recordingQueryBuilder` + `fakeConnectionResolver` que evitam `pdo_sqlite` (TENANT-006)
- `Arqel\Tenant\Scopes\TenantScope` (final, `implements Scope<Model>`) — global scope que lê `TenantManager::current()` via `Container::getInstance()` e adiciona `where(<table>.<tenant_fk>, <id>)` à query. No-op gracioso quando container não bind `TenantManager`, sem tenant, sem `getQualifiedTenantKeyName()`, ou coluna não é string. `Arqel\Tenant\Concerns\BelongsToTenant` trait — `bootBelongsToTenant()` registra TenantScope + listener `creating` que auto-fill `<tenant_fk>` quando null. Foreign key resolve via 3-tier fallback: `$tenantForeignKey` property → `config('arqel.tenancy.foreign_key')` → `'tenant_id'`. `tenant()` BelongsTo (lança `LogicException` se `arqel.tenancy.model` não configurado). Query scopes `withoutTenant()` e `forTenant(Model|int|string)`. `phpstan.neon` ganha ignore para `trait.unused` em `packages/tenant/src/Concerns/*` (false positive). 14 testes novos (76 total, 113 assertions): foreign key config/override/fallback, auto-fill creating happy/skip, scope registered, scope no-op/with-tenant, forTenant id/Model, withoutTenant. Test scaffolding via `fireCreating(Model)` que dispara `eloquent.creating` event sem precisar DB (TENANT-005)
- `Arqel\Tenant\Middleware\ResolveTenantMiddleware` (final) — `handle(Request, Closure, string $mode='required')` invoca `TenantManager::resolve()` antes do controller, lança `TenantNotFoundException` quando required+missing, deixa passar quando optional. Constantes `MODE_REQUIRED`/`MODE_OPTIONAL`; `normaliseMode()` case-insensitive + trim-tolerant; valores desconhecidos caem em `required` por segurança. `Arqel\Tenant\Exceptions\TenantNotFoundException` (não-final, apps podem extend) com `__construct($message, ?string $identifier)` e `render(Request)` em 3 shapes: JSON 404 (quando `expectsJson()`), Inertia `arqel::errors.tenant-not-found` (quando view publicada — gate `function_exists('inertia')` + `view()->exists()`), plain Symfony 404 fallback. `TenantServiceProvider::packageBooted` registra alias `arqel.tenant` → `ResolveTenantMiddleware` no Router. 8 testes Pest novos (62 total, 98 assertions): let-through happy, throws on required+missing, optional lets-through, unknown mode→required (safe default), mode case/trim tolerant, JSON render com payload `{message, tenantIdentifier}`, plain 404 fallback sem Inertia, alias registrado (TENANT-004)
- `Arqel\Tenant\TenantManager` (final) reescrito de stub para implementação completa: `resolve(Request)` memoiza per-request (resolver chamado só 1×); `set(?Model)` override programático com dispatch correto (`TenantResolved` em set distinto, `TenantForgotten` em set(null)); `forget()` drop+emit; `runFor(Model, Closure)` swap+restore via `try/finally` (resilient a exceções); `current()`/`currentOrFail()` (lança `LogicException`)/`hasCurrent()`/`id()` (int|string|null com narrow scalar)/`identifier()` (delega ao resolver via `identifierFor`, fallback `(string) id()`)/`resolved()`. Construtor aceita `?TenantResolver` + `?Dispatcher` — apps sem tenancy continuam funcionais via `set()`/`runFor()` direto. 2 events `Arqel\Tenant\Events\TenantResolved` + `TenantForgotten` (final, `public readonly Model $tenant`). `TenantServiceProvider::packageRegistered` agora binda 2 singletons: `TenantResolver` lendo `arqel.tenancy.{resolver,model,identifier_column}` do config (valida `class_exists` + `is_subclass_of`, retorna null em config ausente/inválido) + `TenantManager` resolvido com resolver+Dispatcher do container. 22 testes Pest novos (54 total, 86 assertions): TenantManagerTest cobre init state, resolve sem resolver, resolver delegation+memoise count, dispatch positivo/negativo, set/set(null)/forget eventos, idempotent set, runFor swap+restore (incl. restore on throw), currentOrFail throws/returns, id() int+string keyType, identifier() delegate/fallback/empty (TENANT-003)
- `Arqel\Tenant\Contracts\TenantResolver` interface (`resolve(Request): ?Model` + `identifierFor(Model): string`) e `Arqel\Tenant\Resolvers\AbstractTenantResolver` base com validação `is_subclass_of(Model::class)`, tracking `modelClass`/`identifierColumn`, `identifierFor` default + `findByIdentifier(string)` protected helper. 5 resolvers concretos não-final em `src/Resolvers/`: `SubdomainResolver` (centralDomain + heurística leftmost label, www rejeitado, case-insensitive), `PathResolver` (primeiro segmento + `ignoreSegments` case-insensitive), `HeaderResolver` (X-Tenant-ID via Symfony HeaderBag), `SessionResolver` (`hasSession()` guard + coerção scalar→string + non-scalar rejection), `AuthUserResolver` (convenção Jetstream `currentTeam`: aceita `BelongsTo`/`Model`/property + `instanceof` check). 28 testes Pest novos (32 total, 41 assertions); estratégia DB-less via subclasses anônimas que sobrescrevem `findByIdentifier`. SKILL.md atualizado refletindo TENANT-001..002, com nota explícita justificando `class` (não-final) nos resolvers como extensão esperada (TENANT-002)
- Esqueleto do pacote `arqel/tenant` (Phase 2 — primeiro ticket): PHP 8.3+, Laravel 12|13, dep em `arqel/core` via path repo. `Arqel\Tenant\TenantServiceProvider` auto-discovered + `Arqel\Tenant\TenantManager` (singleton stub com `current(): mixed`/`hasCurrent(): bool` no-op). `composer.json` PSR-4 + autoload-dev + scripts test/coverage/analyse/lint/format. `tests/TestCase.php` Orchestra Testbench com `defineEnvironment` SQLite in-memory; 4 testes Feature smoke (boot, autoload, singleton binding, stub reporta no-current). `phpunit.xml` config padrão Arqel. SKILL.md PT-BR com Status (entregue + por chegar TENANT-002..015) + 3 Anti-patterns. Pacote registrado em `composer.json` raiz e symlinkado via `packages/*` path repo (TENANT-001)
- `@arqel/hooks` test coverage expandida de 4 → 30 testes Vitest. Files novos: `useTable.test.tsx` (8 — sort default/explicit/clear, filters add/remove/clear, selection toggle/all/clear/isSelected), `useFlash.test.tsx` (4 — payload presente, fallback empty, onMessage once-per-new-value, multi-kind dispatch), `useCanAccess.test.tsx` (6 — no auth.can = false, global resolution, record precedence, fallback to global, null/undefined record, non-bool coerced), `useNavigation.test.tsx` (3 — empty, items present, non-array coercion), `useResource.test.tsx` (5 — empty shape, records list, single record, server filters, raw props escape hatch). Mock de `@inertiajs/react` movido para `tests/setup.ts` via `vi.mock` (executa antes de qualquer import — resiste a reordenamento do `organizeImports` Biome). `packages-js/hooks/SKILL.md` § Status — HOOKS-002..006 entregues, "Por chegar" reduzido a Phase 2 (Zod validation, URL sync, progress events reais) (HOOKS-002..006)
- User-aware Action serialization no payload Inertia: `Arqel\Core\Support\InertiaDataBuilder::serializeMany` aceita opcionalmente `?Authenticatable $user` e via `ReflectionMethod::getNumberOfParameters` passa-o para `Action::toArray($user, $record)` quando a assinatura aceita ≥1 param. Helper privado `callToArray` centraliza a inspeção de signature; column/filter ainda recebem chamada zero-arg. Permite resolução user-aware de `disabled`/`url` no payload das listas globais `actions.row`/`actions.bulk`/`actions.toolbar` (ACTIONS-007)
- `arqel/actions` test coverage expandida de 30 → 49 testes (140 assertions): `Unit/ConfirmableTest.php` (8 — default, requiresConfirmation, modalHeading/Description/ConfirmationRequiresText auto-activate, modalColor com fallback destructive, getConfirmationConfig shape), `Unit/HasAuthorizationTest.php` (4 — default true, delegação ao Closure, coerção bool, propagação user+record), `Feature/ActionControllerTest.php` (7 — 404 slug, 404 action name, success notification, deny via authorize → 403, failure notification, 422 sem ids[], duck-typed collection lookup). `tests/Pest.php` ganha `'Unit'` (antes rodava só `Feature/`). `packages/actions/SKILL.md` § Status atualizado — ACTIONS-001..008 entregue (ACTIONS-008)
- `Arqel\Core\Resources\Resource::form(): mixed` (default `null`) — hook opcional simétrico ao `table()`. `Arqel\Core\Support\InertiaDataBuilder::resolveFormFields` (private, duck-typed contra `arqel/form`) detecta presença de `getFields()` + `toArray()` e produz `[fields, formPayload]`. `buildCreateData`/`buildEditData`/`buildShowData` agora emitem chave `form` (= `Form::toArray()`) quando declarado e usam `Form::getFields()` como source do `fields` payload (flatten); sem `form()` declarado, caem no fallback existente (`Resource::fields()` flat, sem chave `form` — zero breaking-change). Normalização de keys via array_values + cast string para satisfazer PHPStan. 5 testes Pest novos (`FormPayloadIntegrationTest`) cobrindo no-form fallback, form declarado, propagação em Edit/Show com record (via `setRawAttributes` evita dep de DB), fallback gracioso para retorno não-objeto. Suite core: 104 → 109 passando, 311 assertions. `packages/form/SKILL.md` § Status atualizado — FORM-006 movido de "Por chegar" para "Entregue" (FORM-006)
- Per-row authorization de row actions no payload Inertia: `Arqel\Core\Support\InertiaDataBuilder::resolveVisibleActionNames` (duck-typed contra `arqel/actions`) emite `record.arqel.actions: ['view', 'edit']` (lista de nomes visíveis para `(user, record)`) avaliando `Action::isVisibleFor` + `Action::canBeExecutedBy`. `serializeRecord` estendido com 2 args opcionais; `buildTableIndexData` resolve `$rowActions` + `$user` uma vez antes do loop. 5 testes Pest novos (`PerRowActionVisibilityTest`) usam Reflection do método privado para testar sem `pdo_sqlite`. Suite core: 99 → 104 passando, 295 assertions (TABLE-007)
- Bulk pipeline confirmado: `ActionController::invokeBulk` + `BulkAction::execute(Collection)` chunking via `chunkSize(int)` (default 100, clamp ≥ 1, callback uma vez por chunk) + teste unit de chunking (250 records → 3 chunks). End-to-end com DB pendente — depende de `pdo_sqlite` driver no host (TABLE-008)
- `packages/table/SKILL.md` reescrito (46 → 130 linhas): § Status atualizado refletindo TABLE-001..008 entregues, exemplo copy-paste completo de `Resource::table()` com 4 columns + 3 filters + 3 actions, seções dedicadas "Per-row authorization" (payload JSON exemplo) e "Bulk pipeline" (sequência de chamadas), 5 anti-patterns
- `tests/TestCase.php` do `arqel/core` ganha `defineEnvironment()` configurando DB SQLite in-memory (isolamento entre Feature tests, sem touch no host filesystem). `tests/Feature/ArqelServiceProviderTest.php` expandido de 6 → 11 testes cobrindo gaps: registro do `arqel:resource` command, `InertiaDataBuilder` como singleton, view namespace `arqel::app` resolvível, translation namespace `arqel::actions.view` em en + pt_BR, mount das 7 rotas polimórficas. Suite total: 99/99 passando, 289 assertions. PHPStan + Pint limpos. Coverage threshold enforced no CI matrix (sem driver local) (CORE-014)
- `packages/core/SKILL.md` § "Adiados" reescrita como "Entregues após o scope inicial" — sincroniza pós-CORE-006/007/010/016 com 5 sub-bullets descrevendo ResourceController + HandleArqelInertiaRequests + FieldSchemaSerializer + InertiaDataBuilder + auto-install frontend (CORE-015)
- `arqel:install` agora instala e configura o lado frontend automaticamente. `Arqel\Core\Commands\InstallCommand` estendido com 4 fases após o scaffold PHP (gated por `--no-frontend`): (1) detect package manager via lockfile, fallback `select()`; (2) `Symfony\Component\Process\Process` invoca `{pm} add @arqel/{react,ui,hooks,fields,types}` + `{pm} add -D @inertiajs/react react react-dom @types/react @types/react-dom` com timeout 300s e TTY auto-detect, verbo correto por pm (`pnpm/yarn add` vs `npm install`), flag dev correto (`-D` vs `--dev`); (3) scaffold `resources/js/app.tsx` de `packages/core/stubs/app.tsx.stub` (com `createArqelApp` boilerplate e `{{app_name}}` substituído); (4) garante `@import 'tailwindcss'` + `@import '@arqel/ui/styles.css'` em `resources/css/app.css` idempotentemente. Property `$processFactory` para test injection. Flag `--force` força re-scaffold; sem ela, idempotente. Skip silente quando `package.json` não existe; falha de rede emite warning não-fatal e continua. 7 testes Pest novos (14/14 total, 39 assertions). `getting-started.md` atualizado: steps 3+4 manuais viraram step 2 unificado (CORE-016)
- 3 guias de migração em `apps/docs/guide/migration/`: `from-filament.md` (mapping side-by-side em 6 tabelas Resource/Fields/Tables/Actions/Layout/Authorization + o-que-não-migra + 2 estratégias), `from-nova.md` (Vue→React mental shift + 5 tabelas de mapping + Tools/Cards stubs Phase 2), `from-react-admin.md` (client-driven vs server-driven com snippets lado-a-lado + quando ainda escreve React + Inertia partial reload em vez de React Query). Sidebar com seção "Migração" e 3 sub-itens. Bonus: react-admin guide adicionado fora do scope original do ticket (ticket pediu apenas Filament + Nova) por positioning value (DOCS-007)
- `apps/docs/guide/agents.md` real cobrindo `AGENTS.md` standard: 4 problemas que resolve para LLMs, 7 seções geradas pelo `arqel:install`, instruções de customização, reprodução via `packages/core/stubs/agents.stub`, MCP stub Phase 2 com 5 tools planeadas (`list-resources`/`get-resource-fields`/`list-actions`/`query-resource`/`inspect-policy`). Sidebar com seção "Integrações" (DOCS-008)
- 5 páginas curadas de API reference TypeScript em `apps/docs/reference/typescript/` (uma por pacote): `types.md` (FieldType discriminated union + FieldSchema canónico + type guards), `react.md` (`createArqelApp` + providers + 3 contexts + utilities), `hooks.md` (10 hooks com signatures + exemplo + nota técnica TS2589 do `useArqelForm`), `ui.md` (todos os components por subpath em tabelas props-chave + FieldRegistry + tokens CSS oklch + `cn()`), `fields.md` (catálogo dos 21 inputs + `FieldRendererProps` shape + `slugify` + side-effect `register.ts` + custom Field triple PHP+React+register). `typescript-overview.md` reescrito como índice com tabela de pacotes + "Convenções gerais" + TODO sobre auto-gen TypeDoc. Sidebar atualizado com 5 sub-itens TS. Auto-gen TypeDoc + TSDoc comments comprehensive pendentes — fica para PR follow-up que adiciona `typedoc --out apps/docs/reference/typescript/_generated packages-js/*/src/index.ts` ao docs-deploy workflow (DOCS-006 parcial)
- 7 páginas curadas de API reference PHP em `apps/docs/reference/php/` (uma por pacote): `core.md`/`fields.md`/`table.md`/`form.md`/`actions.md`/`auth.md`/`nav.md`. Cada uma documenta classes públicas, setters fluentes, oracles, endpoints HTTP e comandos Artisan via tabelas (formato dominante). `php-overview.md` reescrito como índice com tabela de pacotes + seção "Convenções gerais" + TODO sobre auto-geração via phpDocumentor. Sidebar reorganizado em `.vitepress/config.ts` por linguagem (PHP com 7 sub-itens + TypeScript Overview). Auto-geração via phpDocumentor/Doctum + CI regen pendentes — fica para PR follow-up que adiciona `phpdoc -d packages/ -t apps/docs/reference/php/_generated` ao workflow (DOCS-005 parcial)
- Tutorial `apps/docs/guide/tutorial-first-crud.md` (novo) com 10 steps testáveis cobrindo um blog admin completo: migrations (`categories`/`posts` com FK + softDeletes + status), models, `arqel:resource Category/Post --with-policy`, declarações `CategoryResource` (slug `uniqueIn`) e `PostResource` completo (Form com `Section::aside()`, Table com 5 columns + 2 SelectFilter + custom `RowAction publish` com `visible`/`successNotification` + `bulkActions`/`toolbarActions` + `indexQuery` com eager-loading + `beforeCreate` lifecycle), Policy com 5 métodos, registro no Panel, deploy checklist de 6 itens. Sidebar atualizado em `.vitepress/config.ts` com nova seção "Tutorial". `examples/blog-admin.md` simplificado para redirect interno. Critérios "examples/first-crud repo" e "timer < 30 min" pendentes — repo demo fica para PR follow-up (DOCS-004 parcial)
- 5 páginas conceituais reais (substituem stubs DOCS-001) em `apps/docs/guide/`: `panels.md` (Panel mínimo + API fluente em tabela com 12 setters + multi-panel + resolução em runtime), `resources.md` (mínimo + naming conventions + 8 lifecycle hooks + recordTitle/Subtitle + indexQuery + runCreate/runUpdate/runDelete orchestrators), `fields.md` (catálogo dos 21 types em tabela + API fluente comum + validação + visibilidade 4 contextos + dependências com partial reload Inertia + currency PT-BR + macros), `tables-forms.md` (Table builder + 9 column types + 6 filters + Form layout components em tabela + Tabs com badge + FormRequest gerados), `actions.md` (4 variantes + confirmation com type-to-confirm + form modal + bulk com chunking + authorization + action como link). Páginas vizinhas polidas (saíram de stub): `what-is-arqel.md` (filosofia em 3 pilares + stack table + pacotes + comparação Filament/Nova) e `installation.md` (Composer/pnpm commands + Tailwind v4 syntax + path repositories). Containers VitePress (`::: warning`/`::: tip`/`::: details`) usados consistentemente; links internos cruzados (DOCS-003)
- Página `apps/docs/guide/getting-started.md` real (substitui stub de DOCS-001) com 7 steps testáveis: pré-requisitos em tabela, `laravel new`, `composer require arqel/core` + `php artisan arqel:install`, `pnpm add @arqel/*`, config de `app.tsx` (`createArqelApp` + side-effect `@arqel/fields/register` + `@arqel/ui/styles.css`), `php artisan arqel:resource User --with-policy` com `UserResource::fields()` exemplo, `php artisan serve` + `pnpm dev`, login via `tinker`. Seção "Próximos passos" + bloco "Troubleshooting" com 4 warnings (PHP/Node version, permissions storage, `dont-discover`) usando containers VitePress (`::: tip`/`::: warning`/`::: details`). Critérios "screenshot/gif" e "timer < 10 min" pendentes — exigem user real testando (DOCS-002 parcial)
- Site público de documentação `apps/docs/` (`@arqel/docs`, private) com VitePress 1.6.4 + Vue 3.5: `.vitepress/config.ts` PT-BR (nav 5 links + dropdown de versão, sidebar declarativo cobrindo 18 páginas, search local com translations PT-BR, edit-on-GitHub link), theme override em `.vitepress/theme/style.css` (paleta indigo→purple `--vp-c-brand-*`), landing `index.md` (layout `home` com hero + 6 features), 18 stubs PT-BR (`guide/`, `resources/`, `reference/`, `advanced/`, `examples/`) cada um marcado `> **Status:** stub — DOCS-NNN` apontando para o ticket que vai preencher, assets `public/` (favicon.svg/logo.svg/hero.svg) com SVG inline gradiente, `srcExclude: ['**/SKILL.md', '**/README.md']` para evitar VitePress tratar os SKILL.md como páginas indexadas. Build em 2.7s renderiza 19 páginas (DOCS-001)
- Planejamento completo em `PLANNING/` (13 documentos, 328 tickets)
- Convenções operacionais em `CLAUDE.md` e `AGENTS.md`

[Unreleased]: https://github.com/arqel/arqel/compare/HEAD...HEAD
