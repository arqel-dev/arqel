/**
 * @arqel/fields-advanced — advanced field components for Arqel.
 *
 * Each component is registered into `@arqel/ui`'s FieldRegistry by
 * the consuming app (no auto-registration here — host apps decide
 * which advanced fields they want bundled).
 */

export { TagsInput } from './tags/index.js';
