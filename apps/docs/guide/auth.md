# Auth

> **Status:** stub — DOCS-003.

Arqel reusa Policies, Gates e abilities do Laravel sem ACL paralela. `arqel/auth` adiciona `AbilityRegistry`, `EnsureUserCanAccessPanel` e helpers `arqel_can()`. Field-level e action-level authorization são oraculizados via traits `HasAuthorization`. Documentação completa chega em DOCS-003.
