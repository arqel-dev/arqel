# Custom Fields

> **Status:** stub — preenchimento real chega em DOCS-003.

Para criar um field type custom você precisa de três pedaços (PHP class + React component + registo). Veja `packages-js/fields-js/SKILL.md` §"Creating a custom field" enquanto este capítulo é finalizado.
