{
    "name": "arqel/monorepo",
    "description": "Arqel monorepo root — orchestrates PHP packages via Composer path repositories.",
    "type": "project",
    "license": "MIT",
    "homepage": "https://arqel.dev",
    "keywords": [
        "laravel",
        "admin",
        "panel",
        "react",
        "inertia",
        "shadcn"
    ],
    "require": {
        "php": "^8.3"
    },
    "require-dev": {
        "arqel/core": "@dev",
        "larastan/larastan": "^3.9",
        "laravel/pint": "^1.29"
    },
    "repositories": [
        {
            "type": "path",
            "url": "packages/*",
            "options": {
                "symlink": true
            }
        }
    ],
    "autoload": {
        "psr-4": {}
    },
    "autoload-dev": {
        "psr-4": {}
    },
    "scripts": {
        "test": "echo 'no packages yet — sub-packages will expose pest via their own composer.json'",
        "lint": "vendor/bin/pint --test",
        "format": "vendor/bin/pint",
        "analyse": "@php -r \"passthru('bash scripts/phpstan.sh', $c); exit($c);\""
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "config": {
        "sort-packages": true,
        "optimize-autoloader": true,
        "allow-plugins": {
            "pestphp/pest-plugin": true,
            "php-http/discovery": true,
            "composer/package-versions-deprecated": true
        }
    }
}
